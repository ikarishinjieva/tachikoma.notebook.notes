---
note: 01KJBYSC9DM9XPHKKGY9HX1V4E.md
title: 20221102 - 更新内核后, 服务器网卡驱动丢失
indexed_at: 2026-02-27T10:19:35.144267+00:00
---

用户要求我分析这篇关于内核更新后网卡驱动丢失的笔记，并生成索引元数据。让我分析笔记内容：

1. 主题：服务器内核升级后，网卡驱动丢失问题的解决过程
2. 主要内容：
   - 找到Bcom NXE驱动
   - 编译时遇到两个报错
   - 报错1：GCC版本问题，需要升级到gcc-10
   - 报错2：ib_umem_get函数在4.x到5.x内核有变更，需要修改驱动代码
   - 最终编译成功，通过insmod添加模块

3. 标签应该包括：内核升级、网卡驱动、编译问题、Linux、驱动修复等

4. 关联笔记：查看其他笔记列表，主要是MySQL、Clickhouse、性能分析相关的，与这篇网卡驱动问题的笔记关联不大。最可能有关联的是"01KJBYDNK8089C7M3W31CEM4JF.md: 20210628 - 公司上网"可能涉及网络，但关联度很低。其他都是数据库相关的，没有直接关联。

让我生成输出：
## 标签
内核升级, 网卡驱动, 编译修复, Linux, GCC, 驱动兼容

## 摘要
记录服务器内核从 4.x 升级到 5.x 后 Bcom NXE 网卡驱动丢失的修复过程。主要解决两个编译问题：GCC 版本过低导致的 define 语法错误，以及 ib_umem_get 函数签名变更导致的接口不匹配。最终通过升级 GCC 和修改驱动代码完成编译并成功加载模块。

## 关键概念
- ib_umem_get: Linux 内核中用于获取用户内存区域的 RDMA 相关函数，4.x 到 5.x 版本间函数签名发生变更
- GCC 版本兼容: 不同 GCC 版本对 C 语言语法特性支持不同，驱动编译需匹配合适版本
- insmod: Linux 用于动态加载内核模块的命令

## 关联笔记
无
