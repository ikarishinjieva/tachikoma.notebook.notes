---
note: 01KJBZV98B1VCTQQ6ZAB998E7T.md
title: 20250715 - 重新进行SQL优化的GRPO
indexed_at: 2026-02-27T11:41:57.805975+00:00
---

用户要求我分析这篇关于 SQL 优化 GRPO 训练的笔记，并生成索引元数据。让我仔细阅读内容并提取关键信息。

主要内容：
1. 关于 SQL 优化的 GRPO 训练实验记录
2. 使用 JSON 结构增强 LLM 对 SQL 层次的理解
3. 多次训练尝试（尝试 1-5）及遇到的问题（OOM、reward 解析等）
4. 涉及机器资源、代码仓库、训练数据构建等

关键概念：
- GRPO（强化学习方法）
- SQL 优化
- JSON 结构化标注
- Reward 函数
- OOM（内存溢出）

关联笔记：从列表中看，没有直接相关的笔记（其他都是 2021 年的数据库/ClickHouse 相关笔记，而这篇是 2025 年 7 月的 AI 训练相关）

标签应该是：SQL 优化、GRPO、大模型训练、强化学习、JSON 结构化

摘要：记录使用 GRPO 进行 SQL 优化训练的多次实验过程，包括 JSON 结构化方案、训练参数调整及 OOM 等问题排查。

关联笔记：看起来没有直接关联的，因为其他笔记都是 2021 年的数据库底层研究，而这篇是 2025 年的 AI 训练实验。## 标签
SQL 优化，GRPO，大模型训练，强化学习，JSON 结构化，Reward 函数

## 摘要
记录使用 GRPO 对 SQL 优化任务进行强化学习的多次实验过程，包括 JSON 结构化标注方案设计和训练参数调优。尝试 5 次训练迭代，解决了 reward 解析、OOM 等问题，最终在 LR 调整至 1e-4 后 train/reward 曲线表现良好但 eval 未见提升。

## 关键概念
- GRPO: 用于 SQL 优化任务的强化学习方法
- JSON 结构化标注: 将 SQL 转为带编号的 JSON 结构以增强 LLM 对层次的理解
- Reward 函数: 通过比对思考步骤中的变更决定与 GT 变更列表进行评分
- Thinking 步骤: 定义 5 类分析步骤（计算列分析、列引用分析、优化变更、顺序分析、反思）

## 关联笔记
无
