---
note: 01KJBZ2BPVAHM4J5GETGTYP6GT.md
title: 20230805 - chatglm 微调, 对sql进行规则审核 - 增加规则
indexed_at: 2026-02-27T10:33:51.166641+00:00
---

用户要求我分析这篇关于 ChatGLM 微调和 SQL 规则审核的笔记，并生成索引元数据。让我仔细分析笔记内容：

1. **主题**：ChatGLM-6B 模型微调，用于将 SQL 规则转换为 AST 检查规则
2. **核心问题**：
   - 问题 1：模型输出结果不准确，通过增加训练样本量改善
   - 问题 2：规则匹配数据不稳定，尝试去掉特殊符号（...）后结果稳定
3. **技术点**：SQL 规则、AST（抽象语法树）、ChatGLM-6B 微调、训练数据量

**标签**：ChatGLM、微调、SQL 规则、AST、模型训练

**摘要**：记录使用 ChatGLM-6B 模型将 SQL 规则转换为 AST 检查规则的训练过程。通过增加样本量和调整输入格式（去掉...符号）来提高模型输出的准确性和稳定性。

**关键概念**：
- ChatGLM-6B：用于规则转换的大语言模型
- AST 检查规则：基于抽象语法树的 SQL 规则检查方式
- SQL 规则：需要被转换为 AST 检查的 SQL 规范

**关联笔记**：从笔记列表看，这篇笔记主要关于 ChatGLM 微调和 SQL 规则，与其他笔记（主要是 MySQL、Clickhouse、系统性能等）没有直接关联，所以应该是「无」。## 标签
ChatGLM, 模型微调, SQL 规则, AST, 训练数据

## 摘要
记录使用 ChatGLM-6B 模型将 SQL 规则转换为 AST 检查规则的训练过程。通过增加样本量（从 832 增至 3328）和调整输入格式（去掉...符号）来提高模型输出的准确性和稳定性。

## 关键概念
- ChatGLM-6B: 用于 SQL 规则到 AST 检查规则转换的大语言模型
- AST 检查规则: 基于抽象语法树的 SQL 规则检查方式
- 恒等条件检查: WHERE 子句中不能仅使用 city=city 类恒等条件的规则

## 关联笔记
无
