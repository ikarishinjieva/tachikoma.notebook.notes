---
title: 20250415 - 研究SQL优化模型微调 - 重新梳理SQL优化的数据构建过程
confluence_page_id: 3801686
created_at: 2025-04-15T09:30:28+00:00
updated_at: 2025-05-01T17:50:12+00:00
---

# 前继

[20250407 - 研究SQL优化模型微调 - 让模型对SQL的修改 遵循 改写指令]

用DPO让模型遵循改写指令, 然后使用两阶段验证, 验证效果正确: 

  - 阶段1: SQL优化: SQL + 规则 -> 分析流程
  - 阶段2: SQL改写: SQL + 分析流程 -> 优化后的SQL

下一步: 使用两阶段验证, 已经稳定了SQL修改的指令遵循效果. 可以继续优化阶段1的效果, 目标是让SQL优化正确率和规则数上升

# 梳理SQL优化的数据构建过程

  1. /opt/huangyan/sql-ai/v1/1_sql_cluster.ipynb 从若干个SQL数据集中, 抽取聚类中心
  2. /opt/huangyan/sql-ai/v1/2_add_fault_in_sql.ipynb 在聚类中心的SQL中, 增加违反规则的部分
     1. 举例: "在子查询中保留甚至添加外层查询不需要的列"
  3. /opt/huangyan/sql-ai/v2/make_dataset.ipynb 
     1. 对SQL进行结构分析
     2. 在结构中, 增加改写指令, 输出改写后的SQL (但这部分在阶段2没有使用)
     3. 进行多次改写迭代
  4. /opt/huangyan/sql-ai/v2/enhance_data_1.ipynb 生成定向的增强数据
     1. 在聚类中心的SQL中, 增加指定方向的错误SQL片段
        1. 举例: 增加一个join...on... 连接操作, 使用一个有业务意义的表名和列名(不要跟现有的重复)进行连接, 列出你在连接中新增的右表的"表名.列名"
     2. 在结构流程中, 增加定向的分析步骤
        1. 举例: 如果以下描述属实, 那么你需要在"改写分析"中明确写出: 连接列"{added_column}", 是被JOIN使用, 不能删除; 否则, 直接输出"无法满足特殊要求"
  5. 使用3/4步骤的数据进行训练

# 分析之前的优化结果

存在两个问题: 

  - v1.months列的引用没有被正确去除, 判断是LEFT JOIN的左表列 判断出了问题
  - 当前规则中, v2表不会被删除, 可以增加规则, 让不被使用的表被删除 (LEFT JOIN情况下该表不影响结果, 但在其他场景下需要仔细判断)

```
 SELECT AVG(A44_T_1_.fd_roi) AS "T_A81_2_" FROM (
     SELECT v1.months, t1.fd_ggr / v3.fd AS fd_roi
     FROM (
        SELECT SUBSTRING(pt, 1, 6) AS months
         FROM dwd.dwd_order_detail_basic_ri
         GROUP BY SUBSTRING(pt, 1, 6)
     ) v1
     LEFT JOIN (
         SELECT SUM(bingoggr) AS fd_ggr, SUBSTRING(pt, 1, 6) AS months
         FROM dwd.dwd_order_detail_basic_ri
         WHERE login_name IN (
             SELECT login_name FROM dwd.dwd_user_info_ri WHERE substring(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'),1,6 ) =substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6 )
         )
         AND substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6 )=substring(TO_CHAR(pt::timestamp, 'YYYYMMDD'),1,6 )
         GROUP BY SUBSTRING(pt, 1, 6)
     ) t1 ON v1.months = t1.months
     LEFT JOIN (
         SELECT sum(cost) AS cost, state_date AS state_date
         FROM (
             SELECT sum(cost) AS cost, state_date FROM dwd.dwd_market_channel_cost_di WHERE state_cycle='MONTH' GROUP BY state_date
             UNION ALL
             SELECT sum(cost) AS cost, SUBSTRING( TO_CHAR(state_date::timestamp, 'YYYYMMDD'),1,6) AS state_date FROM dwd.dwd_market_channel_cost_di WHERE state_cycle='DAY' GROUP BY
             SUBSTRING( TO_CHAR(state_date::timestamp, 'YYYYMMDD'),1,6)
         ) a
         GROUP BY a.state_date
     ) v2 ON v1.months = v2.state_date
     LEFT JOIN (
         SELECT substring(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'),1,6 ) AS months, count(1) AS fd
         FROM dwd.dwd_user_info_ri
         GROUP BY substring(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'),1,6 )
     ) v3 ON v1.months = v3.months
 ) AS A44_T_1_ LIMIT 1 OFFSET 0 
``` 

  
下一步: 简化训练数据的生成过程, 能便捷地生成针对性的训练数据, 重新设计训练数据的生成过程: 

  1. 遍历 SQL聚类后的中心SQL
  2. 要求增加一个违反SQL规则的片段, 并给出这个片段的key (比如表A的列C)  

     1. 根据第2步的key, 生成 "改写指令X" (比如 "因为其没有被引用, 所以需要删除 表A的列C")
  3. 生成对SQL的结构分析过程
  4. 在结构分析过程中, 进行改写分析, 并增加改写指令X. 
  5. 改成训练数据格式

重构训练数据的生成:

  - commit: 9b84dffd840c9ea9125edbc34166e034cec8a120
  - 脚本: /opt/huangyan/sql-ai/make_sql_analyze_data.ipynb

# 训练1

数据: 

  - 数据量: 82 
    - (在数据生成中, 增加了验证环节, 能通过验证环节的数据比较少, 可能是因为验证比较严苛)
  - 只包括一种劣化: 
    - "随机选一个子查询, 在子查询中增加查询一个列 (列名尽量随机, 并符合业务意义), 该列在外查询中没有被使用"
  - 每个SQL只劣化一次

训练: 

  - LR=1e-4
  - epoch=12

评估结果: [2025-04-16_13-25-17.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-16_13-25-17.log)

  - 第一轮: 
    - 去除三个没有使用列 (正确)
    - 多去除了 列的引号, 以及一个没必要的别名 (没有在分析中, 暂不修复重写引擎)
  - 第二轮: 
    - 改写分析 和 改写指令 不匹配, 改写分析分析的是对的, 但改写指令都为空 (需要修复)
    - 去除了三个没有使用的列 (正确)
    - 将计算列移入了子查询 (没有在分析中, 暂不修复重写引擎, 但这一步移动是错误的, 导致SQL格式错误)
    - 去除了外层引用子查询的表名 (没有在分析中, 暂不修复重写引擎)
  - 第三轮:  

    - 删除了四个列, 有两列正确, 其他两列因为下列分析错误被误删除
      - 由于上一步错误地"将计算列移入了子查询", 所以本步骤的分析是错的
  - 第四轮: 
    - 因为第二轮导致的SQL格式错误 (多了一个残缺的子查询), 本轮删除了months列, 这是错误的
  - 后面结构已经混乱, 不继续分析

# 训练2

先修复"改写分析 和 改写指令 不匹配, 改写分析分析的是对的, 但改写指令都为空".

这一点在重构前的数据训练中没有出现. 所以先检查现在训练数据是否正确, 然后再判断是否因为数据量不足导致.

使用脚本/opt/huangyan/sql-ai/check_sql_analyze_data.ipynb检查结果, 如下: 

```
FAILED:
检查过程：

1. **最外层 (INSERT INTO ... VALUES ...):**  改写分析指出不需要改写，改写指令为“无”。匹配。

2. **目标表 (matches):** 改写分析指出不需要改写，改写指令为“无”。匹配。

3. **插入值 (VALUES ...):** 改写分析指出子查询需要调整，但改写指令为“无”。**不匹配**。这里应该说明 VALUES 子句中包含需要改写的子查询。

4. **子查询 (SELECT winner_id, winner_entry FROM players WHERE player_id = 200004):** 改写分析指出需要删除对 `winner_entry` 的引用，改写指令为“将子查询修改为 `(select winner_id from players where player_id = 200004)`”。匹配。

结论：

由于"插入值 (VALUES ...)" 部分的改写分析和改写指令不匹配，因此 **指令不完全匹配**。

建议：

将 "1.2. **插入值 (VALUES ...):  具体的比赛数据**" 部分的“改写指令: 无” 修改为类似 "需要修改其包含的子查询，删除对 `winner_entry` 的引用" 的描述，使其与分析对应。

--------------------
FAILED:
检查过程：

1. **查询1:**

* 改写分析: 列`flight.departure_time`没有被外查询引用, 可以删除对该列的引用。
* 改写指令: 将内层查询`SELECT flight.airline_code, flight.departure_time` 改写为 `SELECT flight.airline_code`

  --> **匹配**.  分析指出了`flight.departure_time`的多余性，指令也对应地删除了它。

2. **查询2:**

* 改写分析: 内层查询只引用了`flight.airline_code`列, 符合改写要求.

  --> **不匹配**.  这里没有改写指令，但分析却说“符合改写要求”。这说明要么分析多余，要么缺少对应的改写指令。由于查询2的结构与查询1类似，且查询1有改写，推测这里缺少改写指令。合理的改写指令应该是“无需改写”。

3. **查询3:**

* 改写分析: 内层查询只引用了`flight.airline_code`列, 符合改写要求.

  --> **不匹配**.  与查询2的情况相同，缺少“无需改写”的指令。

结论：

指令**不完全匹配**。查询2和查询3缺少与分析对应的“无需改写”指令。

--------------------
FAILED:
检查过程：

1. **最外层查询:**  改写分析指出子查询1中`heartrate`未被使用，建议修改子查询1。改写指令为"无"，但实际应指向修改子查询1。 **不匹配**

2. **子查询 1:** 改写分析指出`heartrate`未使用，建议删除。改写指令给出具体修改方法。 **匹配**

3. **子查询 1.1.1:** 改写分析和改写指令都指出无需修改。 **匹配**

4. **子查询 1.1.1.1:** 改写分析和改写指令都指出无需修改。 **匹配**

5. **子查询 2:** 改写分析和改写指令都指出无需修改。 **匹配**

6. **子查询 1.2.1:** 改写分析和改写指令都指出无需修改。 **匹配**

7. **子查询 1.2.1.1:** 改写分析和改写指令都指出无需修改。 **匹配**

结论：

由于最外层查询的改写分析和改写指令不匹配，因此 **指令不完全匹配**。

--------------------
FAILED:
检查过程：

1. **最外层查询:**  改写分析和改写指令匹配。都没有改写。
2. **衍生表:** 改写分析和改写指令匹配。都没有改写。
3. **WHERE 子句:** 改写分析和改写指令匹配。都没有改写。
4. **计算列 `c1`:** 改写分析和改写指令匹配。都没有改写。
5. **子查询:**  改写分析指出存在无意义的列引用，但没有明确指出是哪个子查询。改写指令明确指出修改中间层子查询，移除`patient.gender`。 由于子查询的描述中包含了中间层子查询的描述和指令，可以认为改写分析和改写指令**不完全匹配**，因为分析部分不够清晰。
6. **最内层子查询:** 改写分析和改写指令匹配。都没有改写。
7. **中间层子查询:** 改写分析和改写指令匹配。都移除了`patient.gender`。

结论：

虽然最终的改写指令在代码层面是正确的，但由于子查询的分析描述不够精确，导致分析和指令之间存在歧义。 因此，**指令不完全匹配**。  需要更正子查询的改写分析，明确指出是中间层子查询包含无意义的列引用。

--------------------
FAILED:
检查过程：

1. **最外层查询:**

   * 改写分析： 指出子查询存在冗余列`continent`需要移除。
   * 改写指令： 无。 **不匹配**。 应该有指令移除第二个子查询中冗余的`continent`列。

2. **第一个子查询:**

   * 改写分析： 指出无需改写。
   * 改写指令： 无。 **匹配**。

3. **第二个子查询:**

   * 改写分析： 指出`continent`列未被使用，可以移除。
   * 改写指令： 指示将`SELECT LENGTH, continent` 改写为 `SELECT LENGTH`。 **匹配**。

结论：

由于最外层查询的改写分析和改写指令不匹配，因此 **指令不完全匹配**。

--------------------
``` 

根据以上偏差, 在生成数据时增加要求和验证: 

3\. "改写分析"和"改写指令"只需要分析当前SQL层次需要进行的改写, 其下一级层次的问题 由下一级的"改写分析"和"改写指令"部分来分析  
4\. "改写分析"和"改写指令"一定要一一匹配

重新生成数据并进行训练

数据: 

  - 数据量: 71 (验证了 改写分析和改写指令的一致性, 导致数据量继续减少)
  - 只包括一种劣化: 
    - "随机选一个子查询, 在子查询中增加查询一个列 (列名尽量随机, 并符合业务意义), 该列在外查询中没有被使用"
  - 每个SQL只劣化一次

验证结果: 

  - 第一轮: 分析中出现了SQL中不存在的列名 (臆想列名), 认为是训练数据量不足引起的
  - 第二轮: 出现了错误的分析: 删除了JOIN...ON...的列, 这个问题在之前的训练数据中解决过, 将针对性的数据构造进来即可

下一步, 解决以上两个问题

# 训练3

  - 生成更多的SQL聚类, 使用脚本: /opt/huangyan/sql-ai/v1/1_sql_cluster.ipynb, 放大四倍SQL量 (40 * 20 个聚类, 总共选取了800个SQL)
  - 生成两类数据: 
    - 第一类: 劣化: "随机选一个子查询, 在子查询中增加查询一个列 (列名尽量随机, 并符合业务意义), 该列在外查询中没有被使用", 不限制数据条数
    - 第二类: 劣化: "增加一个join...on... 连接操作, 使用一个有业务意义的表名和列名(不要跟现有的重复)进行连接, 列出你在连接中新增的右表的\"表名.列名\"", 最多50条
  - 数据量: 
    - 第一类: 293条
    - 第二类: 59条
  - 评估: [2025-04-17_15-42-33.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-17_15-42-33.log)
    - 第一轮: 
      - 删除没有使用的两列, 正确
      - 将计算列中的v3.fd改成了t1.fd, 错误, 改写分析中没有提及
    - 第二轮:
      - 删除没有使用的三列, 正确
    - 第三轮:
      - 分析错误: “删除了`v2.state_date`列的引用”, 这一步没有执行, 且分析不是出现在正确的SQL子查询中, 而是出现在前一个SQL子查询中
    - 第四轮:
      - 错误与第三轮一样

结果: 

  - 没有出现"臆想列名"
  - "改写分析"和"改写指令" 一一匹配

需要:

  - 检查训练数据, 分析中涉及的列 是否出现在正确的章节中
    - 训练数据中, 有一些"改写指令"的段落位置可以改进, 没有明显的错误, 但可以改进
    - 在生成数据的代码中, 增加这个改进
  - 发现: 训练数据中, 很少有"连接列"信息, 主要是因为数据生成中的要求错误, 需要修正
  - 在评估中, 没有出现"是被JOIN使用, 不能删除"这种分析, 进行课程训练试一下, 如果不行, 再查找原因
    - 使用了课程训练, 第二阶段只训练第二类数据, 评估中出现了"连接列"v3.fd", "t1.fd_ggr", "v2.cost", "v3.fd"都是被JOIN使用"
    - 也就是说: 第二类数据数据量不足, 于是在综合训练后不会出现指定分析("是被JOIN使用, 不能删除")

额外:

  - ~~在生成数据的代码中, 增加了对正确性的改写流程, 增加数据正确的概率~~ (测试后无法增加正确的概率)

# 训练4

  - 使用调整后的逻辑, 重新生成数据:
    - commit: d65097e2f8dafaee285b2025a86b01a2ee082f99
    - 数据量: 
      - 第一类: 470条
      - 第二类: 100条
    - 训练参数: LR=1e-4, epoch=8
    - 训练效果: [2025-04-17_22-16-24.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-17_22-16-24.log)
      - 第一轮: 
        - 删除了4个无用列, 正确
        - 删除了v2整个表, 这一点在改写指令中没有提及, 改写指令中仅去除了v2.cost的引用
          - 需要优化阶段2模型: 去除列 并且 表再无用时, 不应删除表
      - 第二轮: 
        - 删除了1个无用列, 正确
      - 第三轮:
        - 删除了1个无用列, 正确
      - 第四轮:
        - 分析中要求删除 计算列的分子列的引用, 也就是说: SELECT t1.fd_ggr, t1.fd_ggr/v3.fd AS fd_roi ..., 要求删除fd_ggr的引用, 要求正确. 但执行错误
          - 执行中, 模型认为需要溢出计算式中的分子, 于是变成了 非计算式 t1.fd_roi. 这一点需要 优化阶段2模型
        - 之后的轮次, 都在这个错误上继续进行
      - 第五轮
        - 由于前一轮的错误, 本轮要求删除 之前计算式的分母 v3.fd
        - 多了一个错误的分析, 模型出现幻觉认为存在 v1.fd, 不影响执行结果, 但这是一个幻觉, 需要优化阶段1模型
      - 第六轮  

        - 认为不需要修改
      - 第七轮  

        - 由于v3.fd之前被删除. 本轮认为v3的连接列也没有被外层使用, 可以移除. 分析正确. 但执行时错误, 删除了整个v3表. 需要优化阶段2模型. 
        - 自动修正了第五轮修改中, t1.fd_roi是t1中不存在的, 于是改成了t1.fd_ggr. 这一点需要 优化阶段2的模型
      - 第八轮
        - 分析错误, 将SELECT...WHERE login_name IN (SELECT login_name FROM dwd.dwd_user_info_ri) 中的login_name认为是外层未使用, 于是移除了这个条件, 改变了SQL的意思
        - 阶段2的模型, 还错误地将 "SELECT sum(bingoggr) AS fd_ggr" 改成了 "SELECT sum(fd_ggr) AS fd_ggr". 原因不明.
      - 第九轮  

        - 移除了一个不用的列, 正确
      - 第十轮  

        - 认为不需要修改

总结: 

  - 对阶段1的模型: 
    - 第5轮出现了一个幻觉, 触发条件不明, 继续观察
    - 第8轮, 将 "不使用的列" 扩展到了条件中的列, 错误地删除了条件, 这方面需要检查训练数据, 或者细化规则的描述
  - 对阶段2的模型:  

    - 第4轮, 当 列和其计算式并存时, 删除列会同时修改计算式, 这一步错误
    - 第1轮 + 第7轮, 移除表最后一个列时, 会同时移除表, 这一步错误
    - 第7轮 + 第8轮, 都进行了一些错误的"修正", 这一步错误  

先处理: 阶段1的模型, 检查训练数据, 是否存在 (将 "不使用的列" 扩展到了条件中的列) 的情况, 用检测脚本. prompt: 

```
"""我有一个对SQL进行改写的分析过程, 我需要你仔细检查: 每一个 根据改写指令 被删除的列, 应当是 在改写前应当是 在某一个查询中被选择的列. 如果检查通过, 则输出"被删除的列在改写前全都是在某一个查询中被选择的列", 否则输出你的结论.

对SQL进行改写的分析过程
-------------------------------------------------------
{analyze_process}
-------------------------------------------------------
"""
``` 

在检测时发现几个额外的问题: 

  - sql_story 没错, 但增加改写指令后, sql_story的SQL样例 已经是 改写后的样例了. 这样会导致训练数据中 原始SQL 和 各步骤的SQL样例不匹配. 需要修正生成算法.
  - 生成算法中需要去除 多语句SQL的处理

重新生成数据, 再次验证数据是否有 (将 "不使用的列" 扩展到了条件中的列), 发现以下问题: 

  - 对于create table语句, 劣化会形成很怪的结果, 应当排除. 只保留SELECT和WITH ... SELECT 在 Rule-1 的劣化中
  - 仍存在一些错误的分析流程, 在改写时修改了引用的SQL. 但数据量比较小 (16/413), 可以在检查时删除这些数据

TODO: 已修正生成数据的脚本, 等待重新生成数据来验证 以上两个问题是否解决

查看评估结果: [2025-04-18_17-19-29.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-18_17-19-29.log)

  - 第1轮: 
    - 删除两个不使用的列引用, 正确
  - 第2轮: 
    - 没有变更
  - 第3轮:
    - 删除两个不使用的列引用, 正确
  - 第4轮: 
    - 出现了一个错误的分析: 
      - \- 计算列说明: `fd_ggr`: 计算列，通过 `SUM(bingoggr)` 计算；`months`: 计算列，通过 `SUBSTRING(pt, 1, 6)` 计算。  
\- 改写分析: 列 `SUBSTRING(pt, 1, 6)` 没有被外查询引用, 可以删除对该列的引用。`WHERE` 子句中的子查询也可以优化。  
\- 改写指令: 删除 t1 子查询中的 `SUBSTRING(pt, 1, 6)` 以及对应的 `GROUP BY` 子句. 将 WHERE 子句中的子查询 `SELECT login_name FROM dwd.dwd_user_info_ri WHERE SUBSTRING(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'), 1, 6) = SUBSTRING(TO_CHAR(current_date::timestamp, 'YYYYMMDD'), 1, 6)` 移动到外层查询, 并将 `IN` 改为 `=`

      - 其上一层的分析中指出: "- 改写分析: 本层没有需要改写的部分。连接列 `v1.months` 和 `t1.months` 是必须的。"
      - 也就是说模型认为 months 和 "SUBSTRING(pt, 1, 6)" 不是同一列? 这一点需要在第一阶段的模型中优化 (GROUP BY 计算式, 计算式 AS 列名)
  - 第5轮
    - 出现了一个错误的分析: 
      - \- **计算列说明**:

\- `fd_ggr`: 使用 `SUM(bingoggr)` 计算每月的 `bingoggr` 总和。

\- `months`: 使用 `SUBSTRING(pt, 1, 6)` 提取月份信息。

\- **过滤条件**: 筛选特定用户的订单数据，且 `current_date` 和 `pt` 都在同一个月。

\- **改写分析**: 列"pt"没有被外查询引用, 可以删除对`pt`的引用.

\- **改写指令**: 删除 `pt` 列, 并删除 `SUBSTRING(pt, 1, 6)`.

      - 删除了计算列中的成分列, 这一点需要在第一阶段的模型中优化

  - 后面的轮次不继续分析

  

用脚本检查训练数据中是否有删除计算列或成分列, 发现如下问题: 

  - 模型对"计算列"的理解有误, 一些JOIN右表的列被标记成了计算列, 比如:   

    - \- **作用**: 查找所有属于“科幻”类别的电影的`film_id` 和类别最后更新时间`category.last_update`。
    - \- **计算列**: `film_id`, `category.last_update`.

  - 训练数据中会删除计算列的成分列, 并且不顾计算是否正确 (需要约束模型不能改变SQL的运行结果)

  - 劣化时的SQL会出错, 比如: "OR B.Price <= ( SELECT AVG ( Price ) , A.Region FROM wine )", 需要在劣化时规定SQL的正确性? 
  - 删除列引用时, 除了选择列, 还会从GROUP BY中一起删除该列引用, 改变了SQL的含义. 需要扩写规则, 从"删除列引用" 变成 "从选择列表中减少选择的列"

对生成数据的脚本进行增强: 

  - 添加对计算列的解释, 明确"计算列是基于其他列的值通过表达式计算生成的列，而不是直接存储在表中的数据"
  - 生成训练数据时, 要求改写前后不能改变SQL运行结果, 并增加一个检查环节
  - 劣化时, 要求保障SQL的正确性 (但没有增加检查环节)
  - 将Rule1的描述修改成""从子查询的选择列表中, 删除在外查询中没有被使用的列选择"

使用重新生成的训练数据进行训练

# 训练5

  - 使用调整后的逻辑, 重新生成数据:
    - commit: b5e3ebe921c68236942801d4071be0b4fbfb1446
    - 数据量: 
      - 第一类: 222条 (增加了正确性验证, 数量大大减少)
      - 第二类: 100条
    - 训练参数: LR=1e-4, epoch=8
  - 评估效果: 
    - 第1轮: 
      - 增加了一个多余的条件: 
        - \- **改写分析**: 外层查询只使用了`fd_roi`列，其他列都没有使用。因此，子查询中`v1`、`t1`、`v2`、`v3`的冗余列都可能导致子查询性能下降。

\- **改写指令**: 在所有`LEFT JOIN`中，将连接条件`v1.months=v2.state_date` 改写为`v1.months=v2.state_date AND v2.state_date IS NOT NULL`.

      - 删除了一个不使用的列, 正确

    - 第2轮: 
      - 删除了一个不使用的列, 正确
    - 第3轮:
      - 分析认为不需要进行改写
      - 执行时在选择中增加了v1.valid_account, 意义不明. 认为是阶段2的模型问题.
    - 第4轮:
      - 将上一轮增加的列删除, 正确
    - 第5轮:
      - 删除了一个不使用的列, 正确
    - 第6轮:
      - 将外选择中, v1.months的选择删除了, 正确
      - 但: 额外将v1子查询删除了 (没有意识到v1.months是连接列), 并改变了其他相关的连接列 (在分析中并没有出现)
        - * **删除了`v1`子查询**: diff中完全删除了`v1`子查询及其相关的`LEFT JOIN`条件 (`ON v1.months = t1.months`, `ON v1.months = v2.state_date`, `ON v1.months = v3.months`)。

* **修改了`v2`和`v3`的`LEFT JOIN`条件**: `v2`的连接条件从`v1.months = v2.state_date` 变成了 `v2.state_date = t1.months`; `v3`的连接条件从`v1.months = v3.months` 变成了 `v3.months = t1.months`。

    - 第7轮: 
      - 改写指令要求: 从最外层的SELECT中, “删除第三个匿名表的选择列表中的`fd`列”. 分析正确, 但未被执行
    - 第8轮:
      - 与上一轮的要求相似, 但正确执行
      - 与第3轮类似, 增加了t1.months列的选择 (在分析中并没有出现)
    - 第9轮:
      - 没有变更

对于阶段1模型来说, 只存在一个问题, 就是第1轮增加了一个多余的条件. 其他的都是阶段2模型的问题, 尝试将阶段2模型换成gemini-1.5-pro, 测试效果.

# 训练6

重新生成数据, 将"改写指令"要求为"从xxx变成xxx"这种形式. 重新训练后进行评估. 并将阶段2模型换成gemini-1.5-pro

评估效果: 

  - 评估结果不稳定, 出现了"没有改写指令", 以及"改写指令超出规则"的几种随机的现象
  - 评估日志: [2025-04-20_13-18-56.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-20_13-18-56.log)
  - 第1轮: 
    - 不需改写
  - 第2轮: 
    - 删除最外层使用的3个列, 正确
  - 第3轮:
    - 改写指令将v2中两层子查询进行了合并
    - 有一个分析(但不是指令): 
      - \- `LIMIT 1 OFFSET 0`: 这两个关键字组合在一起的意思是跳过0条记录后取1条记录。在这里的主要作用是限制结果集只返回一条平均ROI值，可能是为了后续扩展或数据分析做准备。 原SQL中的`LIMIT 1 OFFSET 0`并没有影响最终结果，因为外层查询只取了一条平均值，删除后意义相同.
      - 这个分析被阶段2实际执行了, 不影响结果
  - 第4轮:
    - 不需改写
  - 第5轮:
    - 分析要求删除两个列(但v1.fd不存在), 并实际执行正确删除了列:
      - \- 改写分析: 子查询中列"fd", 因为没有被使用, 可以删除.  
\- 改写指令: 将子查询 A44_T_1_ 修改成 SELECT months, fd_ggr, v3.fd, fd_roi FROM ... (移除冗余的 v1.fd 和 v1.valid_account)

    - 但根据分析给出的代码片段, 将v1.months改成了months, 这一点认为是"移除冗余的 v1.fd" 带来的错误变更?
  - 第6轮:
    - 删除最外层使用的3个列, 正确
  - 第7轮:
    - 不需改写
  - 第8轮:
    - 分析中没有给出任何指令
    - 在分析中, 有一处SQL片段与原SQL不同, 导致执行时, 将"v1 子查询中的 `sum(bingoggr)` 被改写成了 `sum(bingoggr) / count(1)`", 错误
  - 第9轮: 从第8轮错误的结果上继续
    - 出现了错误的分析和执行, 都是错误的:
      - 分析是错误的(计算列的成分列被删除?), 执行也是错误的(删除了连接列, 是因为表连接的分析没有提到具体列?)  
\- 改写分析: 列选择"v3.fd"没有被外查询引用, 可以删除该列选择。   
\- 改写指令: 将 `SELECT sum(bingoggr) / count(1) as fd_ggr, substring(pt,1,6) as months`, 修改成 `SELECT sum(bingoggr) / count(1) as fd_ggr`

    - 在分析中, 还有一处SQL片段与原SQL不同
  - 第10轮:
    - 不需改写

对阶段1模型的问题进行总结:

  - 不稳定: 需要增加数据量?
  - 有改写指令将 子查询进行了合并, 需要检查训练数据
    - 存在一个类似的数据: "改写分析: 子查询中列选择\"patient.gender\"没有被外查询引用, 可以删除该列选择. `IN`子查询可以直接合并到主查询中，使用`JOIN`更高效."
    - 需要增加一个检查步骤, 就是改写指令中只能包含指定规则
  - 存在两处: SQL片段与原SQL不同
    - 增加检查步骤
  - 删除了连接列, 是因为表连接的分析没有提到具体列
    - 在生成连接分析时, 要求增加"表.列"以及业务意义的描述
  - 计算列的成分列被删除
    - 增加新的增强规则: 
      - 第三类数据:

```
    {
        "id": 3,
        "violate_desc": """1. 在当前查询中添加一个公式计算列, 这个计算列由2-3个列通过运算得到结果, 并增加别名(需要有业务含义)。
2. 在最外面新增一层父查询
3. 在父查询中引用这个子查询中新增的计算列的别名。
4. 最后，输出你用于构造计算列的任意一个成分列的完整名称（“表名.列名”格式，如果适用）或列名""",
        "violate_key": "构造计算列的任意一个成分列的完整名称",
        "violate_analyze": "列\"{violate_value}\", 因为是一个计算列的成分, 不能删除",
        "optimize_rule": "从子查询的选择列表中, 删除在外查询中没有被使用的列选择",
        "allowed_sql_regex": r"^\s*(SELECT|WITH)\b",
        "rule_checker": None,
        "max_data": 50
    }

``` 

修改生成数据的算法, 并且 切换模型deepseek-v3-0324来生成数据试一下数据质量 (但其中有一个环节使用了gemini, 保证效果)

# 训练7

  - 使用调整后的逻辑, 重新生成数据:
    - commit: 51966000a134b348bac6c904e492db267d065082
    - 数据量: 
      - 第一类: 236条
      - 第二类: 50条
      - 第三类: 50条
    - 训练参数: LR=1e-4, epoch=8
  - 评估效果: (temperature=0.6)
    - 第1轮: 
      - 删除最外层一个不使用的列, 正确
      - 有一个额外的变更, 是因为原SQL和分析流程的SQL片段不一致
    - 第2轮:
      - 删除了最外层5个不使用的列, 正确, 已达到最简
      - 删除了第二层的1个不使用的列, 正确
    - 第3/4轮: 无变更
      - 未将不使用的列完全清理:   

        - LEFT JOIN最左表的多余列, 没有清除
        - 子查询中的未使用列, 没有清理
  - 重做一次评估
    - 第1轮:
      - 删除最外层一个不使用的列, 正确
    - 第2轮:
      - 删除最外层2个不使用的列, 第二层1个不使用的列, 正确但没用的
    - 第3轮:
      - 删除了"v2.state_date", 错误
        - 分析中指出: 连接条件 `v1.months=x.months` 是必要的，用于关联不同表的月份数据
          - 用x指代了多张表, 但在v2的分析中并没有提及连接列
    - 第4轮:
      - 删除最外层一个不使用的列, 正确
      - 出现了 (特殊要求确认: 列"v1.months"没有被外查询引用，可以删除该列选择), 该分析是正确的
    - 第5轮:
      - 没有变更
    - 第6轮:
      - 删除最外层一个不使用的列, 正确
      - 分析中会有额外的"特殊要求确认: 列选择"text"确实没有被外查询引用，可以删除该列选择", 该分析是错误的
    - 第7/8轮: 无变更
      - 未将不使用的列完全清理: 
        - 计算列和计算成分列同时出现在SELECT中, 没有删除 计算成分列
        - LEFT JOIN最左表的多余列, 没有清除
        - 子查询中的未使用列, 没有清理

总结: 

  - 对于连接列的分析, 都放在了父表上, 导致分析子表的时候没有连接列的信息, 分析错误
    - 在生成数据的算法中, 增加要求: 
      - 4\. 在每个章节中, 需要分析当前层次中 表连接(如有) 中参与连接的列, 以及其连接的业务意义   
5\. 在参与表连接的表的分析章节, 也需要分析当前表中 参与表连接的列

  - 分析过程中会出现 "特殊要求确认" 
    - 训练数据中存在"特殊要求"的情况, 需要修正
  - 原SQL和分析流程的SQL片段仍会不一致, 得验证训练数据是否存在情况, 并修正
    - 在生成数据过程中, 增加相关要求
  - 子查询中的未使用列, 没有清理  

    - 增加evaluate时的重试次数, 待观察
  - 计算列和计算成分列同时出现在SELECT中, 没有删除 计算成分列  

    - 待观察 (只出现了一次)
  - LEFT JOIN最左表的多余列, 没有清除
    - 待观察 (有可能是"子查询中的未使用列, 没有清理"的影响)

# 训练8

  - 使用调整后的逻辑, 重新生成数据:
    - commit: c5fe5eedd9e0888410e47c59b9a72a6da1e2452b
    - 数据量: 
      - 第一类: 222条
      - 第二类: 50条
      - 第三类: 50条
    - 训练参数: LR=1e-4, epoch=8
  - 评估
    - 日志: [2025-04-22_04-27-04.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-22_04-27-04.log)
    - 第1轮: 
      - 删除最外层4个不使用的列, 正确
    - 第2轮: 
      - 删除最外层2个不使用的(计算列的成分列), 正确
    - 第3轮: 
      - 删除第2层1个不使用的列, 正确
    - 第4轮:
      - 没有变更
    - 第5轮: 
      - 删除v1.bingoggr列, 正确
      - 删除t1.months (连接列), 错误
        - 对t1的分析, "参与连接的列' 写错了, "改写分析"中也没有提及不能删除参与连接的列
          - 参与连接的列:  
\- `v1.months`：用于与v1的months列连接

    - 第6轮: 
      - 删除v2.state_date (连接列), 错误
        - 缺少对连接列的分析
    - 第7轮:
      - 删除了v3.fd (外层计算列的成分列), 错误
        - 对计算列的分析中, 缺少列分析?, 导致v3中没有意识到外层对fd的使用
          - 计算列分析:  
\- `t1.fd_ggr/v3.fd as fd_roi`：计算每日中奖金额/用户数量作为月度roi指标

    - 第8轮: 
      - 没有变更
    - 第9轮: 
      - 删除了t1的列别名fd_ggr, 改写分析和指令不一致, 两者都是错误的: 
        - **改写分析：**  
列选择"v3.fd"没有被外查询引用，可以删除该列选择

**改写指令：**  
将 'select sum(bingoggr) as fd_ggr', 修改成 'select sum(bingoggr)'

    - 第10轮: 
      - 没有变更

总结: 

  - 需要指定"表连接分析", "参与连接的列"的格式, 列出参与的两列, 并且在子查询的分析中, 指出参与连接的列不能删除
  - 需要制定"计算列"的格式, 列出参与计算的列, 并且在子查询的分析中, 指出参与计算的列不能删除
  - 改写分析和指令不一致的现象, 没有规律, 持续观察

考虑更改数据生成的算法, 改成逐层生成试试

# 更改数据生成的算法 - 逐层生成, 训练9

  - 先分析层次结构和筛选条件
  - 然后添加 "表连接分析", "参与连接的列"
  - 然后添加 "计算列分析"
  - 然后总结被外层使用的列

  - 使用调整后的逻辑, 重新生成数据:
    - commit: 10ab5a934ccfca5dbd4443a8326dad10dcbb8e02
    - 数据量: 
      - 第一类: 330条
    - 训练参数: LR=1e-4, epoch=8
  - 评估: 
    - 第1轮: 
      - 没有变更
      - 需要修正: 在"改写分析"增加要求, 分析过连接条件列和计算列, 还需要分析其他未提及的列
    - 第2轮: 
      - 最外层查询, 删除没有使用的6列, 正确, 且完全
        - 分析中只删除了3列, 阶段2多删除了3列, 需要修正阶段2模型
      - 删除了v2表
        - 分析中指出: "cost列虽然出现在SELECT中，但未被主查询使用，可以考虑删除整个连接或改写"
      - 问题: 以上两项修改, 都是在父查询中指出的, 而不是在对应子查询中指出, 需要修正
    - 第3轮: 
      - 删除了t1的连接列和GROUP BY, 错误
        - t1的连接列, 是在父查询中被删除的
        - t1查询中错误删除了WHERE的子条件, 错误
    - 第4轮:
      - 删除了第二层v1.valid_account, 正确
    - 第5轮: 
      - 分析错误: "- v1子查询中的sum(bingoggr)可以改为sum(bingoggr) as fd_ggr，因为t1子查询使用了这个列", 改写也是错的
    - 第6轮: 
      - 删除了v1和v3的连接列, 错误
        - 删除的决定, 是父查询做出的
        - v1子查询中的分析认为: pt列未被外查询使用，可以删除
        - v3子查询分析正确, 认为不能进行修改
    - 后面的轮次就不做分析了
  - 总结: 
    - 对子查询的改写, 需要在子查询中进行, 而不是父查询中
      - 增加了要求: 
        - 注意: 每个章节的"改动分析"中的内容, 应当只改写了当前章节对应的SQL, 而对于其子查询的改写应当放在对应的子章节中. (这一要求的目的是: "改写分析"中的内容的位置是不会引起误解的)
      - 没有增加检查, 通过实验, 能避免大部分问题, 但会少量遗漏, 待观察效果
    - 在"改写分析"增加要求, 分析过连接条件列和计算列, 还需要分析其他未提及的列
      - 增加了要求: 对于以上未检查的列选择, 需要逐个检查
    - 计算列作为连接列?
      - 检查原始SQL: 

```
sqls = read_json_from_file("/opt/huangyan/sql-ai/dataset/1_sql_cluster.medoid_sqls.json")
for sql in tqdm(sqls[:], desc="Processing"):
    if not ("join" in sql.lower() and "on" in sql.lower()):
        continue
    prompt = f"""分析一下SQL, 其是否存在以下情况: 
- SQL中需要存在 显式地 显式地 显式地JOIN...ON...连接, 显式地 显式地 显式地 使用`ON`关键字
- 在JOIN...ON...中, ON需要明确指定条件
- 在ON的条件式中, 使用了计算列, 而不全部是原生列

如果三个条件都满足, 则输出: "三个条件都满足" (严格遵守以上文字), 否则输出 "没有满足所有条件"(严格遵守以上文字). 你还需要输出你的分析

我的目标是对SQL进行严格的检查, 你必须严格使用以上条件进行检查

SQL:
========
{sql}
========
"""
    resp = call_ai(prompt, temperature=0.1)
    if "三个条件都满足" in resp:
        print("-" * 20)
        print(sql)
        print("-" * 20)
        print(resp)

```

      - 是存在不少用计算列作为连接列的训练数据  
  

    - 需要修正阶段2的模型, 其指令遵循仍不好
      - 先搁置

先修正前两个问题, 修改生成数据的算法

# 训练10

  - 使用调整后的逻辑, 重新生成数据:
    - commit: d40c0a37a0f4afc27a7f0269a5e69c91e595fb2b
    - 数据量: 
      - 第一类: 316条
    - 训练参数: LR=1e-4, epoch=8
  - 训练效果:
    - 第1轮: 
      - 删除了最外层6个不使用的列, 正确
        - 分析中认为可以删除3列, 但给出的代码示例只删除了2列
        - 分析和执行不匹配, 分析自己也冲突
      - 删除了第二层v1的两个不使用的列, 正确
        - 分析和执行正确
      - 删除了表v2
        - 分析中没有提到删除v2
    - 第2轮: 
      - 没有实际变更
      - 在外查询中, 分析认为 可以删除 v1.fd_ggr, 实际上已经删除过这一列了, 而且在外查询中分析这个结论, 也已经越权
    - 第3轮:
      - 没有实际变更
      - 对v3的分析: 认为"可以删除fd列", 但在父查询中, 已经分析出"v3.fd列被用于计算fd_roi，不能删除", 两者冲突
    - 第4轮
      - 没有实际变更
    - 第5轮
      - 没有实际变更
      - 对v1的分析: 认为"可以删除pt列，因为它没有被外部查询使用", 在改写时, 又指出"实际上这个子查询已经是最简形式，不需要改写"
      - 对t1的分析: 跟v1类似

总结: 

  - 需要改善阶段2的模型提示词, 让其严格遵守改写分析
  - 第5轮对pt列的分析, 认为是对规则的描述误解 (pt列知识参与了计算, 并没有参与SELECT), 尝试将"列"改成"选择项", 重新描述规则
  - 第3轮, 父子查询的结论相反, 需要检查训练数据是否存在这种情况, 如果存在, 那需要多步生成(先填写父查询的分析, 然后复制到子查询的分析)
  - 第1/2轮, 分析产生了事实错误, 或自相矛盾. 不确定怎么处理

重新评估一次, 验证其确定性: 

  - 第1轮: 
    - 没有实际变更
    - 其分析中, 认为: 最外层应当删除的列, "在结果中显示，不能删除",
  - 第2轮:  

    - 删除了最外层6个不使用的列, 正确
      - 分析中认为可以删除5列, 但给出的代码示例只删除了3列
      - 分析和执行不匹配, 分析自己也冲突
    - 删除了第二层v1的两个不使用的列, 正确
      - 分析中认为 "子查询v1的结果将用于主查询和所有连接条件" "不需要删除任何列"
      - 分析和执行不匹配
  - 第3轮:   

    - 没有实际变更, 分析与执行一致
  - 第4轮:  

    - 没有实际变更
    - 分析中认为 "pt列仅用于提取月份片段，在上层查询中未被使用", "可以删除pt列选择". 但改写后的分析中认为 "分析显示t1子查询已经是最简形式，没有多余的列可以被删除。我将保持原样不动，因为GROUP BY子句中已经只包含months列，这是上层查询用来连接的唯一列。" 两者冲突
  - 第5轮:  

    - 没有实际变更, 分析与执行一致
  - 第6轮:  

    - 没有实际变更, 分析与执行一致

需要做的修改: 

  - 需要改善阶段2的模型提示词, 让其严格遵守改写分析
    - 用gemini-2.5-pro-preview-03-25模型进行阶段2
  - 第5轮对pt列的分析, 认为是对规则的描述误解 (pt列知识参与了计算, 并没有参与SELECT), 
    - 尝试将"列"改成"选择项", 重新描述规则
  - 第3轮, 父子查询的结论相反, 需要检查训练数据是否存在这种情况, 如果存在, 那需要多步生成(先填写父查询的分析, 然后复制到子查询的分析)
    - 将分析流程结构化, 看看是否能增强模型对层次的感知, 减少父子查询间的分析冲突
  - 第1/2轮, 分析产生了事实错误, 或自相矛盾. 
    - 不确定怎么处理  
  

# 训练11

  - 使用调整后的逻辑, 重新生成数据:
    - commit: bac78f82bc5f755d27ff6e7e3715df8c2494ff0c
    - 数据量: 
      - 第一类: 292条
    - 训练参数: LR=1e-4, epoch=8
  - 评估: 
    - 第1轮: 
      - 没有变更
      - 错误的分析: "A44_T_1_子查询中，主查询只使用了fd_roi列，但v1子查询的bingoggr和valid_account列在后续计算中被使用，因此不能删除。t1子查询的fd列在后续计算中被使用，因此不能删除。v2子查询的cost列在后续计算中被使用，因此不能删除。v3子查询的fd列在后续计算中被使用，因此不能删除。months列用于连接条件，因此不能删除。因此A44_T_1_子查询不需要改写。", 导致没有发生变更
    - 第2轮: 
      - 删除了外层查询的两列, 分析和执行正确
    - 第3轮: 
      - 删除了外层查询的cost, 分析和执行正确
      - 删除了v2表, 分析中未提及
    - 第4轮:
      - 没有变更
      - 分析和执行正确
    - 第5轮:
      - 没有变更
      - 分析和执行正确
    - 第6轮:
      - 删除了外层查询的两列, 分析和执行正确
      - 删除了v1.valid_account, 分析和执行正确
    - 第7轮:
      - 没有变更
      - 分析和执行正确
    - 第8轮:
      - 没有变更
      - 分析和执行正确
    - 第9轮: 
      - 增加了一些注释, 没有实际变更
      - 分析和执行正确
    - 第10轮: 
      - 没有变更
      - 分析和执行正确
  - 再次评估
    - 第1轮
      - 删除最外层的不使用的两列
      - 分析和执行正确
    - 第2轮
      - 没有变更
    - 第3轮
      - 删除最外层的不使用的一列
      - 分析和执行正确
    - 第4轮
      - 删除最外层的不使用的一列
      - 分析和执行正确
    - 第5轮
      - 删除最外层的不使用的两列
      - 分析和执行正确
    - 第6轮
      - 删除v1的不使用的两列. 分析中并没有信息, 错误
      - 删除v2的不使用的一列, 并清理其中的子查询中的相关列. 分析中并没有信息, 错误
    - 第7轮
      - 没有变更
      - 分析中指出要删除pt列, 但变更后发现没有变化
    - 第8轮
      - 没有变更
      - 分析和执行正确
    - 第9轮
      - 没有变更
      - 分析和执行正确
    - 第10轮
      - SQL结构分析很简单
      - 删除了v1的GROUP BY, 但在分析中并没有指出
      - 只给了一段分析:
        - A44_T_1_子查询的计算列fd_roi是主查询使用的唯一列，因此其他列可以被删除。改写规则：删除v1、t1、v2、v3子查询中未被外层查询使用的列。

测试其他case, 使用孙健的case集: <https://github.com/sqlgenius/sqlflash-ft/blob/sunjian_step_5/test/test_simple_one.py>

  - case 1: [2025-04-25_14-36-30.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-25_14-36-30.log)
    - 结果正确, 中间有一步, 将删除的列又加回来, 后续又删除
  - case 2: 是之前一直使用的case
  - case 4: [2025-04-25_22-13-53.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-25_22-13-53.log)
    - 第一步分析错误, 删除了使用中的列, 第二轮又将这些列加回来, 最终结果正确
      - 需要分析错误原因: 子查询中的分析跟父查询中的信息对不上
  - case 6: [2025-04-25_15-15-20.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-25_15-15-20.log)
    - 结果正确
    - 第一步分析中, 分析和改写的SQL不一致
    - 第二部分析中, 提到了T1.price, 这是一个不存在的列
  - case 7: [2025-04-25_15-27-07.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-25_15-27-07.log)
    - 结果正确, 并给出了超额的优化
    - 第1步, 执行比分析多删了一列
    - 第2步, 使用了其他规则的优化, 分析中没有提及
  - case 11: [2025-04-25_15-34-28.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-25_15-34-28.log)
    - 结果正确
  - case 12: [2025-04-25_15-47-31.log](/assets/01KJBZSH34T3S96WRRSDA6EVVJ/2025-04-25_15-47-31.log)
    - 结果错误, 跟case 2结构很像: v1.bingoggr没有删除, 多删除了v3.fd
      - 分析中有冲突: 
        - 父查询: "检查其他列: v1.months和v3.fd都在连接条件中使用，不能删除",
        - v3: "fd未被A44_T_1_子查询使用，可以删除"

修改: 

  - 在生成数据时, 在json结构中明确要求列出"修改前后"的SQL片段进行对比
  - 增加检查: 子查询中的分析跟父查询中的信息是否能对齐, 如果有错误, 那么进行修正

# 训练12

  - 使用调整后的逻辑, 重新生成数据:
    - commit: fce75edc1b50b3aa1d8b037b34c53df42ca0edd0
    - 脚本: /opt/huangyan/sql-ai/make_sql_analyze_data.json_format.ipynb, 现在的流程: 
    - 数据量: 
      - 第一类: 310条
    - 训练参数: LR=1e-4, epoch=8
  - 评估:   

    - 改写分析中, 分析类似于"检查v1子查询的选择列v1.bingoggr是否在A44_T_1_中被使用", 应当是明确的"分析结果", 需要修改
    - 分析的结论和分析不匹配: 
      - "结论": "因为v1.bingoggr和v1.valid_account都在A44_T_1_中被使用，且v1.months未被使用，不需改写"
      - case
        - "分析1": "t1子查询返回fd_ggr和months两列，但主查询只使用了fd_ggr的除法结果和months连接",
        - "需要做的改写总结": "可以保留months列用于连接，但fd_ggr应作为计算列保留",
    - "改写分析"改成"对规则匹配程度的分析"试试
    - 分析的不完备: "分析1": "v2子查询只返回cost、state_date两列，state_date被用于连接" (只讲了半句话)
    - 错误的分析:

```
"改写分析": {
    "分析1": "主查询只使用了fd_roi列，但v1、v2、v3子查询都贡献了months列",
    "分析2": "t1子查询的fd_ggr和v3子查询的fd在主查询中未被直接使用",
    "分析3": "v2子查询的state_date在主查询中未被直接使用",
    "结论": {
        "需要做的改写总结": "可以删除未被直接使用的fd_ggr、fd和state_date列",
        "改写前": "select v1.months, t1.fd_ggr/v3.fd as fd_roi from (...) v1 left join (...) t1 on v1.months=t1.months left join (...) v2 on v1.months=v2.state_date left join (...) v3 on v1.months=v3.months",
        "改写后": "select v1.months from (...) v1 left join (...) t1 on v1.months=t1.months left join (...) v2 on v1.months=v2.state_date left join (...) v3 on v1.months=v3.months"
    }
}
```

  - 以上几个问题, 都可以通过"逻辑完整性"来检查:   

```
我有一个SQL和对SQL的分析过程, 我需要你逐个检查 各章节的"改写分析": 
1. 其中的"分析"是否是自洽的
2. 其中的"分析"从中文语法的角度, 是否是完备且明确的
3. 其中的"分析"是否存在逻辑完整性缺失

用中文告诉我你的分析.
在分析完成后:
- 如果有错误, 那么你需要对错误进行修改 (注意: 只对错误进行修改, 不要加入其他非必要的内容). 输出"检查过程和结论"和"需要做的修改". 
- 如果没有错误, 输出"检查过程和结论", "需要做的修改"置为空数组

```

    - 使用如上检查, 在训练数据中可以检查出相关的问题
    - 增加对逻辑完整性的修正, 重新生成数据

# 训练13

  - 使用调整后的逻辑, 重新生成数据:
    - commit: 7e086b776c9f380c18241ef3731b618f59da275d
    - 数据量: 
      - 第一类: 269条
    - 训练参数: LR=1e-4, epoch=8
  - 评估: 
    - 做出了错误的判断, 删除了选择列中的参与连接的列: "外查询只使用了fd列(用于计算fd_roi)，未使用months列"

  - 生成数据时, 要求对重点进行检查: 
    - \- 根据其中的改写内容, 在改写后是否会导致表连接的错误  
\- 根据其中的改写内容, 在改写后是否会导致表计算列的错误  
\- 根据其中的改写内容, 在改写后是否会导致其他错误

# 训练14

  - 使用调整后的逻辑, 重新生成数据:
    - commit: 18aed3d535527d353d06bc6490dee12462118acf
    - 数据量: 
      - 第一类: 280条
    - 训练参数: LR=1e-4, epoch=8
  - 评估: 
    - case 1:
      - 错误删除了连接列, 出现了冲突的分析: 
        - "参与连接的列": [  
{  
"列名": "movie_id",  
"连接用途": "用于与子查询2(filtered_movies)进行连接"  
}

        - "分析2": "主查询未使用all_users.movie_id"
      - 错误删除了外部计算列的成分列, 出现了冲突的分析: 
        - "参与计算的表和列": [  
"all_users表的user_id列"  
],

        - "分析2": "子查询1中的user_id列在主查询中未被使用"
    - case 2:
      - 结论中存在模糊的描述: "需要做的改写总结": "删除主查询中未使用的列选择，但保留fd_roi列作为最终输出"
      - 错误删除了外部计算列的成分列, 出现了冲突的分析:
        - "参与计算的表和列": [  
"t1子查询的fd_ggr列(来自dwd_order_detail_basic_ri表)",

        - "结论": "因为fd_ggr列未被外部查询使用，可以删除",

      - 错误删除了外部计算列的成分列, 出现了冲突的分析:
        - "参与计算的表和列": [  
"t1子查询的bingoggr列(赌局收益)",  
"v3子查询的fd列(用户存款)"

        - "分析结果2": "fd列在后续查询中未被使用"
    - case 3:
      - 错误删除了连接列, 出现了冲突的分析:
        - "参与连接的列": [  
{  
"列名": "MF_ORDER_NO",  
"连接用途": "用于与主查询中的E.AG_ORDER_NUMBER列关联"  
}  
]

        - "需要做的改写总结": "VEHICLE_ID_7和MF_ORDER_NO在主查询中未被使用，可以删除",

总结: 

  - 比较好的现象: 同一个SQL在多次生成中, 会有概率正确或错误, 意味着模型的认知
  - TODO
    - 调整"连接分析"和"参与连接的列"的逻辑, 只保留连接分析
    - 在生成和审查阶段, 要求对上一级使用的列等进行详细说明
    - 将"查询"和"查询语句"两个概念区分开, 需要统一术语
      - 使用术语: "语句" 和 "结果字段"

# 训练15

  - 使用调整后的逻辑, 重新生成数据:
    - commit: d3eefe9131959148772b3312d2f1feca8bbcf5b9
    - 数据量: 
      - 第一类: 261条
    - 训练参数: LR=1e-4, epoch=8
  - 评估: 
    - case 1:
      - 出现分析冲突: 

```
"改写分析": {
...
                        "分析结果2": "这些列中的user_id被用于主查询的连接条件，rating_score被用于主查询的WHERE条件",
...
                        "结论": {
                            "需要做的改写的总结": "从all_users子查询的SELECT语句中删除rating_score列。保留rating_score列用于主查询的WHERE条件。",
...
                        }
                    }

```

    - case 2:
      - 删除了父查询的计算成分列: 

```
"改写分析": {
    "分析结果1": "t1子查询的SELECT语句返回了两列(fd_ggr和months)",
    "分析结果2": "主查询的SELECT语句只使用了t1.months列",
    "分析结果3": "t1.fd_ggr列未被主查询直接使用",
    "结论": {
        "需要做的改写的总结": "删除t1子查询中未被主查询使用的fd_ggr列",
        "改写前": "select sum(bingoggr) as fd_ggr, substring(pt,1,6) as months from dwd.dwd_order_detail_basic_ri where login_name in (...) and substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6)=substring(TO_CHAR(pt::timestamp, 'YYYYMMDD'),1,6) group by substring(pt,1,6)",
        "改写后": "select substring(pt,1,6) as months from dwd.dwd_order_detail_basic_ri where login_name in (...) and substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6)=substring(TO_CHAR(pt::timestamp, 'YYYYMMDD'),1,6) group by substring(pt,1,6)"
    }
},
```

    - case 4: 
      - 删除了计算列的成分列(SOWU_FLAG), 并且漏分析了当前的计算列 (RN): 

```
"改写分析": {
    "分析结果1": "主查询中使用了VEHICLE_ID_10和MF_ORDER_NO用于连接条件",
    "分析结果2": "主查询中使用了WS_BUSINESS_TYPE字段",
    "分析结果3": "主查询中未使用PRE_SERIES_FLAG和SOWU_FLAG字段",
    "分析结果4": "主查询中未使用TIME_STAMP字段",
    "结论": {
        "需要做的改写的总结": "根据改写规则，删除子查询中在外查询中未被使用的列选择",
        "改写前": "SELECT VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO, WS_BUSINESS_TYPE, PRE_SERIES_FLAG, SOWU_FLAG, TIME_STAMP, ROW_NUMBER() OVER(...) AS RN",
        "改写后": "SELECT VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO, WS_BUSINESS_TYPE, TIME_STAMP"
    }
}
```

        - 对计算列的成分列的判断, 是主查询没有使用. 认为其原因是因为在json输出中, 将父查询中的计算列分析, 放在了子查询的分析的后面 (json的字段顺序问题)
        - "需要做的改写的总结"描述比较模糊

总结: 需要解决的问题: 

  - json输出中, 父查询的分析, 应当写在子查询的分析之前
  - "需要做的改写的总结"描述比较模糊: 

    - 查看了训练数据, 模糊的数据占比很小
  - 需要能正确处理: 子查询的选择列, 是父查询中的计算列的成分列 ()
  - 需要能正确处理: 子查询的选择列, 是父查询中的WHERE的条件列 (分析中发生了逻辑冲突)

发现evaluate的提示词不对, 因为在训练中 区分了"查询"/"语句"/"结果字段", 规则还是要求识别"外查询中未使用的字段", 所以之前评估中的问题是因为规则设定得不对. 

将evaluate中的规则修订为跟训练数据一致: "从子语句的结果字段列表中, 删除没有被其他语句使用的结果字段"

使用epoch=12评估效果 (就不用重新训练一次epoch=8, 但主要测试修订evaluate后的效果, 忽略epoch影响): 

  - case 1:
    - 将子查询中无意义的order by删除

```
"改写分析": {
    "分析结果1": "在filtered_movies子查询中，只有movie_id列被选中并用于与all_users表的连接",
    "分析结果2": "ORDER BY movies.movie_release_year子句是排序条件，不是必须的選項",
    "分析结果3": "movie_id列被用于主查询的连接条件",
    "结论": {
        "需要做的改写的总结": "删除未被使用的排序条件子句",
        "改写前": "SELECT movies.movie_id FROM movies WHERE movies.movie_title = 'The South' ORDER BY movies.movie_release_year",
        "改写后": "SELECT movies.movie_id FROM movies WHERE movies.movie_title = 'The South'"
    }
}
```

    - 要求删除主查询中不存在的列 (在子查询中存在, 怀疑是幻觉), 但实际上没有执行

```
"改写分析": {
                "分析结果1": "主查询的SELECT部分只使用了all_users.user_id进行COUNT聚合，而WHERE条件使用了all_users.rating_score。",
                "分析结果2": "ON条件使用了all_users.movie_id，但该列在主查询的SELECT结果中未被直接引用。",
                "结论": "因为主查询的SELECT部分只使用了user_id进行计数，且rating_score仅在WHERE条件中使用，所以需要删除rating_score列。ON条件使用movie_id列进行连接，该列需要保留在子查询中，因此不需改写。"
            },

```

  - case 2
    - 分析中出现逻辑错误, 但不影响正确性 (将应当删除的列 判定成 不需改写)

```
"改写分析": {
                                "分析结果1": "v1子查询的结果被A44_T_1_子查询使用，提供months、bingoggr和valid_account列",
                                "分析结果2": "bingoggr和valid_account虽然未被主查询直接引用，但months列被用于与t1子查询等进行连接",
                                "结论": "虽然bingoggr和valid_account未被主查询使用，但months列被用于连接操作，不能删除。因此不需改写。"
                            }

```

    - 出现在查询条件中IN的子查询的错误改写, 实际未执行

```
"改写分析": {
    "分析结果1": "结果列login_name不在A44_T_1_子查询的SELECT列表中被使用",
    "分析结果2": "WHERE条件中使用的计算列substring(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'),1,6 )=substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6)在A44_T_1_子查询中仅用于WHERE条件，不直接依赖login_name列",
    "结论": {
        "需要做的改写的总结": "删除未被使用的结果列",
        "改写前": "select login_name from dwd.dwd_user_info_ri where substring(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'),1,6 )=substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6)",
        "改写后": "select substring(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'),1,6 )=substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6) from dwd.dwd_user_info_ri where substring(TO_CHAR(first_deposit_date::timestamp, 'YYYYMMDD'),1,6 )=substring(TO_CHAR(current_date::timestamp, 'YYYYMMDD'),1,6)"
    }
}
```

    - 错误替换了列名:

```
"改写分析": {
    "分析结果1": "该子查询只返回state_date列",
    "分析结果2": "上层子查询的ON条件使用了v1.months列",
    "结论": {
        "需要做的改写的总结": "删除子查询v2的SELECT列表中的a.state_date，并替换成a.months",
        "改写前": "select a.state_date from (...) a group by a.state_date",
        "改写后": "select a.months from (...) a group by a.state_date"
    }
},
```

  - case 4
    - 认为不需要修改, 但实际上可以删除其中一个被计算列使用的成分列:
      - 其中TIME_STAMP应被删除, 但没有删除

```
SELECT
        VEHICLE_ID_7,
        VEHICLE_ID_10,
        MF_ORDER_NO,
        WS_BUSINESS_TYPE,
        PRE_SERIES_FLAG,
        SOWU_FLAG,
        TIME_STAMP,
        ROW_NUMBER() OVER(
            PARTITION BY VEHICLE_ID_7,
            VEHICLE_ID_10,
            MF_ORDER_NO
            ORDER BY
                TIME_STAMP DESC
        ) AS RN
``` 

总结: 感觉上没有太大的错误, 出现了一些不能被执行的逻辑错误, 以及一些遗漏, 但不影响大的正确性

# 训练16

尝试训练Qwen/Qwen3-8B, 需要调低NPROC_PER_NODE=2

  - 使用调整后的逻辑, 重新生成数据:
    - commit: 0e5b6add2d6ee6ad5ae2ee4857e3781f42d2da42
    - 数据量: 
      - 第一类: 261条
    - 训练参数: LR=1e-4, epoch=8
  - 评估: 
    - case 1:
      - 出现幻觉: 
        - 其中movie_release_year没有出现在SELECT列表中, 此处为幻觉, 但删除指令将ORDER BY语句删除了. 对SQL正确性没有影响.

```
                    "改写分析": {
                        "分析结果1": "主语句的ON条件使用了filtered_movies子查询的movie_id字段",
                        "分析结果2": "movie_title字段仅用于子查询的WHERE条件，主语句中未使用",
                        "分析结果3": "movie_release_year字段用于子查询的ORDER BY，主语句中未使用",
                        "结论": "因为movie_title和movie_release_year字段仅在子查询中用于WHERE条件和ORDER BY，主语句中仅使用movie_id字段，所以SELECT列表中的movie_title和movie_release_year字段可以删除"
                    }

``` 
        - 另外, 以上改写分析中, 缺少了改写前后的SQL片段

      - 出现幻觉:
        - movie_title并没有出现在查询字段中

```
"改写分析": {
    "分析结果1": "主语句使用了filtered_movies子查询的movie_id字段与all_users子查询的movie_id字段进行JOIN连接",
    "分析结果2": "movie_title字段仅用于子查询的WHERE条件筛选，未被外部查询使用",
    "结论": {
        "需要做的改写的总结": "可以删除filtered_movies子查询结果字段列表中的movie_title字段",
        "改写前": "SELECT movies.movie_id FROM movies WHERE movies.movie_title = 'The South'",
        "改写后": "SELECT movies.movie_id FROM movies WHERE movies.movie_title = 'The South'"
    }
}
``` 
      - 结果是正确的
    - case 2:
      - 有一处错误的分析, 不影响结果: 

```
"需要做的改写总结": "删除未被使用的v1.months和v2.state_date字段，但由于它们用于连接条件，不能直接删除，应在连接条件中使用\"constant\"函数忽略这些字段"
```

      - 结果是正确的, 但不全面(计算列的成分列, 跟计算列在同一层次, 但没有删除. 属于瑕疵). 结果 收敛很快
    - case 4:
      - 有一处不必要的分析 (要移除RN的ORDER BY, 正确但不是规则要求), 不影响结果:

```
"改写分析": {
                        "分析结果1": "计算列RN仅用于连接条件(RN = 1)且在主查询中不作为结果字段输出",
                        "分析结果2": "字段TIME_STAMP在主查询中未被使用",
                        "结论": {
                            "需要做的改写的总结": "删除未被主查询使用的字段TIME_STAMP，并移除计算列RN的排序表达式",
                            "改写前": "SELECT VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO, WS_BUSINESS_TYPE, PRE_SERIES_FLAG, SOWU_FLAG, TIME_STAMP, ROW_NUMBER() OVER(...) AS RN",
                            "改写后": "SELECT VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO, WS_BUSINESS_TYPE, PRE_SERIES_FLAG, SOWU_FLAG, ROW_NUMBER() OVER(...) AS RN"
                        }
                    }

```

      - 结果是正确的
    - case 6:
      - 正确, 分析一次到位
    - case 7:
      - 分析中, SQL示意给错了: 

```
"子语句": {
                        "t2层": {
                            "SQL示意": "SELECT task_id, status FROM (子查询) AS t3",

```

        - 分析和改写是一致的, 由于SQL示意错了, 所以改写时会破坏SQL的正确性. 并在下一轮进行了修正.
    - case 11:
      - 对于应删除的TIMESTAMP列, 没有删除, 给出了如下分析, 其中将TIMESTAMP列(在计算式中使用) 和 其他在JOIN中使用的列 进行了一起处理 (认为此处是一个错误):

```
"改写分析": {
    "分析结果1": "计算列RN仅用于连接条件(B.RN = 1)，不作为结果字段输出",
    "分析结果2": "字段TIME_STAMP未被主查询使用",
    "分析结果3": "字段VEHICLE_ID_7在连接条件中使用，但在主查询的结果字段中未直接引用",
    "分析结果4": "字段VEHICLE_ID_10在连接条件中使用，但在主查询的结果字段中未直接引用",
    "分析结果5": "字段MF_ORDER_NO在连接条件中使用，但在主查询的结果字段中未直接引用",
    "结论": {
        "需要做的改写的总结": "删除未被主查询使用的字段TIME_STAMP，并移除字段VEHICLE_ID_7、VEHICLE_ID_10和MF_ORDER_NO的结果字段列表，但需保留这些字段在连接条件中使用",
        "改写前": "SELECT VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO, WS_BUSINESS_TYPE, PRE_SERIES_FLAG, SOWU_FLAG, TIME_STAMP, ROW_NUMBER() OVER(partition by VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO order by TIME_STAMP DESC) AS RN FROM PRODZU.BORTPPOD",
        "改写后": "SELECT VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO, WS_BUSINESS_TYPE, PRE_SERIES_FLAG, SOWU_FLAG, TIME_STAMP, ROW_NUMBER() OVER(partition by VEHICLE_ID_7, VEHICLE_ID_10, MF_ORDER_NO order by TIME_STAMP DESC) AS RN FROM PRODZU.BORTPPOD"
    }
}
```

    - case 12: 
      - 少删除了一列, 其分析中出现了幻觉错误: 

```
"分析结果1": "bingoggr字段被A44_T_1_子查询的连接条件使用"
```

  - 总结:
    - 没有严重的错误, 比Qwen2的效果略好
    - 存在一些幻觉

保存当前的脚本:

  - 代码: /opt/huangyan/sql-ai/v4
  - 数据: /opt/huangyan/sql-ai/dataset/20250501

下一步扩展到其他规则:

[附件: rule0003.md]
