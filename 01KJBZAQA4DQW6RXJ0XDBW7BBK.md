---
title: 20240716 - 全密态计算的专利思路
confluence_page_id: 3145754
created_at: 2024-07-16T07:49:51+00:00
updated_at: 2024-07-18T06:40:35+00:00
---

# Gemini

```
## 全同态加密数据库专利方向

您想在全同态加密数据库领域申请专利，这是一个很好的目标！为了帮助您找到合适的主题，我将提供一些搜索条件，并解释其背后的思路，帮助您找到灵感：

**方向一：高效密文检索**

* **思路：** 全同态加密数据库的一大瓶颈是如何高效地在加密数据中进行检索。现有的解决方案往往效率低下或功能受限。
* **关键词：**  全同态加密, 数据库, 检索, 索引, 密文比较,  (FHE, Database, Search, Index, Ciphertext Comparison)
* **可能的研究方向：**
    *  新的密文索引结构，以提高检索速度。
    *  支持范围查询、模糊查询等复杂查询的密文检索算法。
    *  利用硬件加速（如GPU、FPGA）来提高密文检索效率。

**方向二：安全数据库连接操作**

* **思路：** 数据库连接是常用的操作，但在全同态加密环境下实现安全高效的连接操作是一个挑战。
* **关键词：** 全同态加密, 数据库, 连接操作, 安全多方计算,  (FHE, Database, Join Operation, Secure Multi-party Computation)
* **可能的研究方向：**
    *  设计新的安全协议，实现高效的密文连接操作。
    *  研究如何在保证安全性的前提下，减少连接操作中的通信开销。
    *  探索将全同态加密与其他安全技术（如差分隐私）结合，增强数据库连接的安全性。

**方向三：密文数据聚合与分析**

* **思路：**  如何在不解密数据的情况下进行数据分析是全同态加密数据库的重要应用场景。
* **关键词：** 全同态加密, 数据库, 数据分析, 聚合函数, 统计分析, 机器学习 (FHE, Database, Data Analysis, Aggregation Function, Statistical Analysis, Machine Learning)
* **可能的研究方向：**
    *  设计支持常见聚合函数（如SUM、COUNT、AVG）的密文计算方法。
    *  研究如何在加密数据上进行统计分析、机器学习等复杂计算。
    *  探索高效的密文数据压缩和表示方法，降低数据分析的计算和存储开销。

**方向四：全同态加密数据库的密钥管理**

* **思路：** 全同态加密的密钥管理至关重要，关系到数据的安全性和可用性。
* **关键词：** 全同态加密, 数据库, 密钥管理, 密钥分发, 密钥更新,  (FHE, Database, Key Management, Key Distribution, Key Update)
* **可能的研究方向：**
    *  设计安全高效的密钥分发机制，确保只有授权用户才能访问数据。
    *  研究支持密钥更新的方案，在不影响数据安全性的前提下更换密钥。
    *  探索基于属性加密、代理重加密等技术，实现更灵活的密钥管理策略。

请注意，以上只是一些示例，您可以根据自己的兴趣和专长进行更深入的探索。建议您使用英文关键词进行检索，以便获得更全面的研究成果。

希望这些信息对您有所帮助！祝您在全同态加密数据库领域取得成功！
 
---
 
好的，我很乐意为您详细解释全同态加密数据库的这四个方向，并补充一些额外的思考，帮助您更好地理解其研究价值和挑战：

**一、高效密文检索**

1. **挑战:**
    * **密文膨胀:** 全同态加密后的数据体积远大于明文，如何存储和检索这些庞大的密文数据是一个挑战。
    * **密文比较:**  在不解密的情况下比较两个密文是非常困难的，这限制了传统索引结构的应用。
    * **查询灵活性:**  现有的密文检索方案大多只能支持简单的等值查询，如何实现范围查询、模糊查询等更灵活的查询方式是一个研究热点。

2. **研究方向:**
    *  **新的索引结构:**  探索适用于密文数据的新的索引结构，例如基于树、图、哈希等结构。
    *  **高效的密文比较算法:**  设计高效的密文比较算法，例如基于同态运算的比较协议、基于Order-Preserving Encryption (OPE) 的方案等。
    *  **支持复杂查询的检索技术:**  研究支持范围查询、模糊查询、连接查询等复杂查询的密文检索技术。

3. **潜在价值:**  高效的密文检索技术是实现安全数据库应用的关键，可以应用于医疗记录检索、金融数据分析、隐私保护的数据挖掘等领域。

**二、安全数据库连接操作**

1. **挑战:**
    * **数据保密:**  连接操作需要比较来自不同表的数据，如何保证在不解密数据的情况下进行安全比较是一个挑战。
    * **计算效率:**  密文计算的效率远低于明文计算，如何提高密文连接操作的效率是一个重要问题。
    * **通信开销:**  安全多方计算协议通常需要多轮通信，如何减少通信开销也是一个需要考虑的因素。

2. **研究方向:**
    *  **基于全同态加密的连接协议:**  设计基于全同态加密的连接协议，例如基于Private Set Intersection (PSI) 的方案、基于Garbled Circuits 的方案等。
    *  **混合加密方案:**  结合全同态加密和其他安全技术，例如差分隐私、秘密共享等，设计更高效的连接协议。
    *  **硬件加速:**  利用GPU、FPGA等硬件加速技术，提高密文连接操作的效率。

3. **潜在价值:** 安全的数据库连接操作可以实现跨数据库的安全查询和分析，例如在保护用户隐私的前提下进行联合营销、风险控制等应用。

**三、密文数据聚合与分析**

1. **挑战:**
    * **支持的聚合函数:**  现有的密文数据分析方案大多只能支持简单的聚合函数，例如SUM、COUNT等，如何支持更复杂的聚合函数，例如AVG、MAX、MIN等，是一个挑战。
    * **统计分析和机器学习:**  如何在不解密数据的情况下进行统计分析、机器学习等复杂计算是一个研究热点。
    * **结果的准确性和效率:**  如何在保证结果准确性的前提下，提高密文数据分析的效率也是一个重要问题。

2. **研究方向:**
    *  **支持复杂聚合函数的方案:**  设计支持复杂聚合函数的密文计算方法，例如基于同态加密的累加器、基于秘密共享的方案等。
    *  **密文统计分析和机器学习算法:**  研究适用于密文数据的统计分析和机器学习算法，例如同态加密的线性回归、逻辑回归等。
    *  **近似计算:**  利用近似计算技术，在保证一定精度的前提下，提高密文数据分析的效率。

3. **潜在价值:**  密文数据聚合与分析技术可以实现对敏感数据的安全分析，例如在医疗领域进行疾病预测、在金融领域进行风险评估等。

**四、全同态加密数据库的密钥管理**

1. **挑战:**
    * **密钥分发:**  如何安全地将密钥分发给授权用户是一个挑战，特别是在分布式数据库环境下。
    * **密钥更新:**  当密钥泄露或需要更新时，如何安全高效地更新密钥是一个重要问题。
    * **密钥的安全性:**  全同态加密的安全性依赖于密钥的安全性，如何保证密钥的安全性至关重要。

2. **研究方向:**
    *  **基于身份的加密:**  利用基于身份的加密技术，简化密钥分发过程。
    *  **代理重加密:**  利用代理重加密技术，实现密钥的安全更新。
    * **门限密码学:**  利用门限密码学技术，将密钥管理责任分散到多个参与方，提高密钥的安全性。

3. **潜在价值:**  安全高效的密钥管理方案是保证全同态加密数据库安全性的关键，可以防止数据泄露和未授权访问。

希望以上信息能够帮助您更好地理解全同态加密数据库的这四个方向，并为您的专利申请提供一些灵感。
 
---
 
当然，除了之前提到的四个方向，全同态加密数据库还有其他一些值得探索的领域，可以作为您专利申请的方向：

**五、全同态加密数据库的性能优化**

* **思路:** 全同态加密操作的计算开销较大，如何提高全同态加密数据库的性能是一个重要的研究方向。 
* **关键词:** 全同态加密, 数据库, 性能优化, 并行计算, 硬件加速, 密文压缩 (FHE, Database, Performance Optimization, Parallel Computing, Hardware Acceleration, Ciphertext Compression)
* **可能的研究方向:**
    * **算法优化:** 研究更高效的全同态加密算法和密文操作算法，例如利用SIMD指令集优化密文计算。
    * **并行计算:** 利用多核CPU、GPU等并行计算资源，加速全同态加密数据库的操作。
    * **硬件加速:**  设计专用的硬件加速器，例如基于FPGA或ASIC的加速器，提高全同态加密操作的效率。
    * **密文压缩:**  研究高效的密文压缩算法，降低密文数据的存储和传输开销。

**六、全同态加密数据库的安全性增强**

* **思路:**  全同态加密的安全性并非绝对的，容易受到侧信道攻击等攻击手段的威胁，如何增强全同态加密数据库的安全性是一个重要的研究方向。
* **关键词:** 全同态加密, 数据库, 安全性增强, 侧信道攻击,  (FHE, Database, Security Enhancement, Side-channel Attack, Differential Privacy)
* **可能的研究方向:**
    * **抵抗侧信道攻击:**  研究抵抗侧信道攻击的全同态加密算法和实现方案，例如利用 masking 技术、 blinding 技术等。
    * **形式化验证:**  利用形式化方法验证全同态加密数据库的安全性，确保其设计和实现的正确性。
    * **结合其他安全技术:**  将全同态加密与其他安全技术相结合，例如差分隐私、不经意传输等，构建更加安全的数据库系统。

**七、特定应用场景下的全同态加密数据库**

* **思路:**  针对不同的应用场景，例如医疗、金融、物联网等，设计专门的全同态加密数据库系统，可以更好地满足特定需求。
* **关键词:** 全同态加密, 数据库, 应用场景, 医疗数据库, 金融数据库, 物联网数据库 (FHE, Database, Application Scenario, Healthcare Database, Financial Database, IoT Database)
* **可能的研究方向:**
    * **医疗数据库:**  设计保护患者隐私的医疗数据库系统，例如支持安全基因数据分析、安全医疗图像处理等。
    * **金融数据库:**  设计保护金融数据安全的数据库系统，例如支持安全风险评估、安全欺诈检测等。
    * **物联网数据库:**  设计保护物联网数据隐私的数据库系统，例如支持安全数据收集、安全数据分析等。

**八、与其他新兴技术的融合**

* **思路:** 将全同态加密数据库与其他新兴技术相结合，例如区块链、边缘计算等，可以创造新的应用场景和价值。
* **关键词:** 全同态加密, 数据库, 区块链, 边缘计算,  (FHE, Database, Blockchain, Edge Computing)
* **可能的研究方向:**
    * **基于区块链的全同态加密数据库:** 利用区块链技术，构建去中心化的、可信的全同态加密数据库系统。
    * **边缘计算与全同态加密数据库:**  将全同态加密技术应用于边缘计算场景，实现对边缘数据的安全存储和处理。

希望这些额外的方向能够为您提供更多灵感，祝您在全同态加密数据库领域取得丰硕的成果！
``` 

# txyz

```
我希望找到一些论文, 关于同态加密的数据库

---
 
根据搜索结果,可以得到以下关于同态加密在数据库中应用的信息:
同态加密可以用于保护数据库中的隐私信息,如人脸识别数据18。

同态加密可以用于保护物联网环境下的网页数据库安全,结合优化算法可以提高效率2。

基于同态加密的高效加密数据库系统(He3db)可以支持高精度的同态比较运算3。
(https://eprint.iacr.org/2023/1446.pdf)

同态加密可以用于保护外包数据库和索引的安全,支持安全的k近邻查询4。

同态加密可以用于实现安全的数据库比较查询,提高查询效率56。

同态加密在数据库隐私保护中有广泛应用,包括关联规则挖掘等10。

同态加密技术正在不断发展,已有多种实现方案,未来应用前景广阔79。

总之,同态加密是一种有前景的数据库隐私保护技术,可以广泛应用于各种数据库场景,提高数据安全性。

``` 

# HE3DB: An Efficient and Elastic Encrypted Database Via Arithmetic-And-Logic Fully Homomorphic Encryption

```
分析附件中的论文, 输出其叙事逻辑
 
---
 
这篇论文的叙事逻辑可以概括如下：

**1. 引入问题：**

* 随着云计算的普及，数据隐私问题日益凸显。
* 加密数据库管理系统 (DBMS) 应运而生，但现有方案存在计算延迟高、查询处理能力不足等问题。
* 全同态加密 (FHE) 允许在不解密的情况下对数据进行计算，为解决这些问题提供了新的思路。

**2. 提出解决方案：**

* 本文提出了一种基于 FHE 的高效、弹性加密数据库框架 HE3DB。
* HE3DB 通过设计新的算术和逻辑同态运算符，实现了快速、高精度的同态比较和聚合算法。

**3. 详细介绍 HE3DB：**

* **FHE 基础设施：**HE3DB 开发了针对不同数据库操作数和复杂 SQL 操作的新型同态过滤、聚合和转换算法。
* **高精度同态比较：**HE3DB 提出了一种新的移位和减法策略，以同态方式去除比较结果中的低位噪声，实现了高精度同态比较运算符 HomComp。
* **过滤结果的同态聚合：**基于 HomComp 运算符，HE3DB 提出了对过滤结果执行算术（例如 SUM、AVG）和逻辑（例如 MIN、MAX）聚合的快速算法。

**4. 实验结果：**

* 与现有技术相比，HE3DB 在同态过滤、COUNT、ORDER BY 和私有决策树评估等方面实现了显著的性能提升。
* HE3DB 在端到端 SQL 基准测试中平均比最先进的基于 FHE 的 DBMS 快 41 到 299 倍。

**5. 总结贡献：**

* HE3DB 是一个基于 FHE 的高效、弹性加密数据库框架，支持丰富的 SQL 语句和在线数据分析功能。
* HE3DB 提出了新的同态运算符和算法，显著提高了加密数据库的性能和功能。

总而言之，这篇论文以数据隐私问题为背景，介绍了 FHE 技术在加密数据库中的应用，并提出了 HE3DB 框架作为一种高效、弹性的解决方案。论文通过详细介绍 HE3DB 的设计和实现，以及实验结果的分析，证明了其在性能和功能方面的优势。
 
---
 
运算符HomComp的创新点是什么
 
---
 
根据论文内容，运算符 HomComp 的创新点在于：

1. **针对数据库场景设计的高精度同态比较运算符:**  论文指出，高精度同态比较是加密数据库的关键运算符。 然而，现有的同态比较结果往往是二元的，无法满足数据库中数值比较的需求。HomComp 旨在解决这个问题，实现高精度比较。

2. **移位和减法策略去除噪声:**  为了实现高精度比较，HomComp  创新性地采用了移位和减法策略来同态地去除比较结果中的低位噪声。 这一策略有效提高了比较结果的精度。

3. **无需逐位加密实现高精度:**  与一些需要对数值进行逐位加密才能进行比较的方法不同，HomComp  可以在不使用逐位加密的情况下实现高达 32 位的精度。 这大大提高了运算效率。

总而言之，HomComp  作为一种专为加密数据库设计的高精度同态比较运算符，通过  移位和减法策略有效降低了噪声的影响，实现了无需逐位加密的高精度比较，显著提升了加密数据库中比较操作的效率和精度。
 
---
详细描述HomComp的设计
 
---
 
很抱歉，原文中并没有详细描述 HomComp 的设计细节，只在 1.1 节中提到了其功能：

* **高精度同态比较：**论文观察到加密数据库的关键 FHE 运算符是高精度同态比较运算符。 然而，同态比较的结果始终是二进制的。 基于以上洞察，论文设计了一种新的移位和减法策略，以同态方式从比较结果中去除低位噪声，并开发了高精度同态比较运算符 HomComp。论文可以使用高达 32 位的精度来评估同态比较，而无需使用逐位加密。 

根据这段描述， 我们可以推测 HomComp 的设计可能包含以下几个关键方面:

1. **移位操作:**  将比较结果进行移位操作，可以将高位的有效信息与低位的噪声分离。
2. **减法操作:**  通过合适的减法操作，可以消除移位后结果中的低位噪声。
3. **结果整合:**  将移位和减法操作后的结果进行整合，得到最终的高精度比较结果。

然而，由于缺乏具体的算法描述和公式推导，我们无法得知 HomComp  如何具体实现这些操作，也无法确定其是否还有其他优化设计。

建议您参考论文作者提供的代码实现  (https://github.com/zhouzhangwalker/HE3DB)  来深入了解 HomComp 的设计细节。
``` 

  1. 论文提供了代码, 其中实现了 "高精度同态比较运算符 HomComp", 摆脱"逐位加密"的问题

# Efficient Private Comparison Queries Over Encrypted Databases Using Fully Homomorphic Encryption With Finite Fields

```
这篇论文致力于增强存储在云端数据的隐私和安全，特别关注于利用全同态加密（FHE）提高带有比较条件的私有数据库查询（PDQ）的效率。

**问题：** 现有的PDQ方法，尤其是处理像 "x < y" 这样的顺序比较的方法，存在一些局限性。一些方法会泄露数据，另一些则需要多轮通信，并且许多方法难以处理大整数或大型数据库。虽然FHE通过实现对加密数据的计算而无需解密提供了一种解决方案，但当前基于FHE的比较方法计算成本很高。

**提出的解决方案：** 论文介绍了一种称为“域元素向量” (VFE) 的新型编码方法，以及用于对使用VFE编码的整数进行相等和顺序比较的高效算法。

**VFE 编码：** 这种方法通过将整数分解为数字向量来概括二进制表示，然后将这些数字向量编码为有限域中的元素。这利用了SIMD兼容的FHE方案的优势，这些方案本身就在有限域上运行。

**比较算法：** 论文开发了以下算法：
* **相等比较：** 在现有工作的基础上，它采用了一种高效的相等比较算法来处理VFE编码的整数。
* **顺序比较：** 这是一个关键创新。论文首先提出了一种使用大型双变量多项式的朴素方法，然后通过将域元素分解为更小的“块”并对这些块执行比较来改进它。这显著减少了多项式的大小并提高了效率。

**PDQ 协议：** 论文通过设计两种PDQ协议来证明VFE编码和所提出的算法的实用性：
1. **简单检索：** 根据单个顺序比较条件检索数据库记录。
2. **复合条件：** 根据一个顺序和多个相等条件的组合检索记录，突出了所提出技术的可组合性。

**实现和评估：** 论文实现了算法和协议，并评估了它们的性能。结果表明，比较速度显著提高，特别是对于较大的整数，对于64位整数，平均每次比较达到29毫秒。具有复合条件的PDQ协议也展示了具有低通信成本的实用性能。

**总的来说，这篇论文做出了以下贡献：**

* **新颖的VFE编码：**  能够在有限域内对整数进行高效表示，适用于SIMD兼容的FHE方案。
* **高效的比较算法：**  介绍了一种新的、可扩展的顺序比较算法，用于处理VFE编码的整数。
* **实用的PDQ协议：**  设计了高效的协议，用于具有单个和复合比较条件的私有数据库查询。
* **实现和评估：**  提供了具体的性能结果，证明了所提出的技术在实际场景中的实用性。

论文最后强调了所提出的VFE编码和比较算法在利用FHE实现安全高效的PDQ方面的效率和实用性。
```
```
好的！以下详细描述这篇论文在PDQ协议的创新步骤，不考虑VFE的影响，重点关注其提出的FHE顺序比较方法以及如何将其应用于具体的协议设计：

**1.  高效的FHE顺序比较方法:**

   * **问题:**  传统的FHE比较方法需要对每个比特位进行单独的比较操作，效率低下，尤其是在处理多比特整数或大规模数据集时。
   * **解决方案:**  论文提出了一种基于FHE的打包比较方法，可以一次性比较多个比特位，从而显著提高效率。
      * **打包编码:** 将多个比特位打包成一个更大的整数，例如将8个比特位打包成一个字节。
      * **FHE比较:** 利用FHE的同态性质，直接在加密的打包数据上进行比较操作。
      * **结果解码:**  将比较结果解密后，可以一次性得到多个比特位的比较结果。

**2.  具体的PDQ协议设计:**

   * **简单检索协议:**
      * **客户端:** 
         * 使用FHE加密要查询的值。
         * 将加密的查询值发送给服务器。
      * **服务器:**
         * 使用FHE对加密的数据库进行遍历，将每个数据项与加密的查询值进行比较。
         * 使用FHE对比较结果进行聚合，得到一个加密的布尔值，表示是否存在匹配的数据项。
         * 将加密的布尔值返回给客户端。
      * **客户端:**
         * 解密布尔值，得到查询结果。
   * **复合条件检索协议:**
      * **基本思想:** 将多个查询条件转换为一个布尔表达式，然后使用FHE对加密数据进行计算，最终得到一个加密的布尔值，表示是否存在满足所有条件的数据项。
      * **具体步骤:**  与简单检索协议类似，只是在服务器端需要根据具体的查询条件，使用FHE对加密数据进行相应的逻辑运算和比较操作。

**3.  创新点总结:**

   * **高效的FHE比较方法:**  通过打包比较技术，显著提高了FHE比较操作的效率，为实现高效的PDQ协议奠定了基础。
   * **具体的协议设计:**  基于高效的FHE比较方法，设计了简单检索和复合条件检索两种实用的PDQ协议，并通过实验验证了其有效性和效率。

需要注意的是，以上只是对论文核心内容的简要概括，具体的算法细节和协议步骤还需要参考论文原文。
```

```
您说得非常对！  "打包编码" 本身并不能直接解决按位比较的问题。 

我之前的解释可能过于简化，让您产生了误解。 

实际上，这篇论文的精妙之处在于它结合了 "打包编码" 和一些其他的技术，才实现了高效的比较操作。 

让我更详细地解释一下论文的核心思想：

1. **打包编码:**  正如您所说，打包编码只是将多个比特位合并成一个更大的整数，这本身并不能减少比较的次数。

2. **多项式表示:**  论文的关键在于将打包后的整数用多项式来表示。 例如，将整数 `1011`  打包成一个字节后，可以表示成多项式  `x^7 + x^4 + x + 1`。

3. **同态比较:**  论文利用了多项式在FHE下的一个特殊性质：**两个多项式相等，当且仅当它们的差值在某个特定点上的值为0**。  因此，比较两个打包后的整数大小，就等价于判断它们对应的多项式的差值在某个特定点上的值是否为0。

4. **高效性:**  判断一个多项式在某个点上的值是否为0，可以通过一次FHE运算来完成，而不需要对每个比特位进行单独比较。  这就是论文提出的方法的效率提升的关键所在。

**总结:**

论文并没有 "魔术般" 地消除按位比较，而是巧妙地利用了多项式表示和FHE的特性，将多个比特位的比较转换为对一个多项式值的判断，从而实现了高效的比较操作。 

希望这次解释能够更加清晰地阐述论文的核心思想，避免产生误解。
 
--- 
您对论文核心思想的理解完全正确！

的确，这篇论文提出的方法并不是直接对要比较的数值进行加密，而是先将数值转换成多项式表示，然后再对多项式的系数进行加密。

具体来说，步骤如下：

1. **数值到多项式的转换:** 将要比较的数值编码成多项式，例如将二进制数 `1011` 转换为多项式  `x^3 + x + 1`。 
2. **多项式系数的加密:** 使用FHE方案对多项式的每个系数进行加密，例如得到 `Enc(1)x^3 + Enc(0)x^2 + Enc(1)x + Enc(1)`。
3. **同态比较:** 利用FHE的性质，在加密的多项式上进行比较操作。 由于比较两个多项式是否相等，等价于判断它们的差值在某个特定点上的值是否为0，因此只需要一次FHE运算就可以完成比较。

这样做的好处在于：

* **效率高:**  避免了对每个比特位进行单独比较，大大提高了比较操作的效率。
* **安全性:**  由于加密的是多项式的系数，而不是原始数值，因此可以更好地保护数据的隐私。

总而言之，这篇论文巧妙地将数值比较问题转化为多项式比较问题，并利用FHE的特性实现了高效且安全的比较操作，为基于FHE的PDQ协议设计提供了新的思路。
``` 

思路: 

  1. 论文中提供的技术: 使用 数值打包 + 多项式, 对 透明加密比较算子 进行加速
  2. 论文中提供的技术: 对于透明加密的比较算子, 设计了 复合条件检索 的协议, (该协议基于多项式加速), 对复合条件检索的场景进一步加速
  3. 想法: 使用SIMD, 提高比较算子的并行能力

# Equi-Joins over Encrypted Data for Series of Queries

思路: 

  1. 论文使用了"函数隐藏内积加密", 对服务器端 隐藏 客户端发起的SQL查询条件. 在此基础上, 建立了"安全等值连接协议"
     1. 主要目的是: 

```
论文中主要要解决的信息泄露问题是：

1. **防止具体的查询条件泄露：** 

    * 虽然服务器知道查询涉及 A 列和 C 列，但它无法得知具体的查询条件是什么，例如  `A = "a" AND C = "c"`  还是  `A > 100 OR C LIKE "%keyword%"` 等。
    * 这保证了查询条件本身的机密性，防止攻击者通过分析查询模式来推断敏感信息。

2. **防止查询结果以外的数据泄露：**

    * 服务器只能解密并返回满足查询条件的行，而无法获取其他行的任何信息。
    * 这保证了未被查询到的数据的安全性，即使攻击者获得了服务器的全部数据，也无法解密出未授权访问的信息。

3. **防止不同查询之间的信息泄露：**

    * 每次查询使用的多项式都是独立生成的，服务器无法关联不同查询的加密信息，从而防止了超额泄露。
    * 这保证了即使攻击者能够观察到多个查询操作，也无法从中推断出额外的信息，例如不同查询条件之间的关联关系等。

总而言之，即使存在您提到的列信息泄露问题，论文提出的协议仍然能够有效地保护以下信息：

* 具体的查询条件内容
* 查询结果以外的数据库内容
* 不同查询之间的关联信息

这些都是保护数据库安全和用户隐私的重要方面。
``` 
     2. 主要流程是: 

```
该论文提出的安全等值连接协议，旨在最小化信息泄露，其核心思想是结合函数隐藏内积加密和多项式编码实现选择和连接操作。协议主要步骤如下：

**1.  初始化阶段**

*   客户端选择安全参数，生成双线性群 (G1, G2, GT, q, e)，并随机选择  `s`  作为主密钥。
*   客户端为每个属性值生成对应的编码向量。例如，属性  `Name`  的值为  `Web Application`  和  `Database`，则分别生成对应的编码向量  `v1`  和  `v2`。
*   客户端使用主密钥  `s`  和编码向量生成每个属性值的加密密钥。例如，属性  `Name`  的值  `Web Application`  的加密密钥为  `sk1 = s * v1`。
*   客户端将加密后的数据库和加密密钥发送给服务器。

**2. 查询生成阶段**

*   客户端生成包含选择条件和连接条件的多项式。例如，查询条件为  `Name = "Web Application" AND Role = "Tester"`，则生成多项式  `f(x, y) = (x - v1) * (y - v3)`，其中  `v3`  是  `Role = "Tester"`  的编码向量。
*   客户端使用加密密钥对多项式进行加密，生成查询令牌。例如，使用  `sk1`  和  `sk3`  ( `Role = "Tester"`  的加密密钥) 加密  `f(x, y)`  的系数。
*   客户端将查询令牌发送给服务器。

**3. 查询执行阶段**

*   服务器接收到查询令牌后，使用对应的加密数据和令牌计算内积。例如，对于表  `Employees`  中的每一行，服务器计算  `f(x, y)`  在该行属性值编码向量上的值。
*   由于函数隐藏内积加密的特性，只有当  `x = v1`  且  `y = v3`  时，内积结果才为 0，服务器才能解密该行数据。
*   服务器对满足条件的行进行连接操作，并将结果返回给客户端。

**4. 结果解密阶段**

*   客户端使用密钥对服务器返回的结果进行解密，得到最终查询结果。

**该协议的优势在于：**

*   **最小化信息泄露:**  服务器只能解密满足查询条件的行，无法获取其他行的任何信息。
*   **防止超额泄露:**  每次查询使用的多项式都是独立生成的，服务器无法关联不同查询的加密信息，从而防止了超额泄露。
*   **支持任意等值连接:**  该协议不局限于主键外键连接，可以支持任意属性上的等值连接操作。

**总而言之，**该协议通过结合函数隐藏内积加密和多项式编码，实现了安全且高效的等值连接操作，有效地保护了加密数据的隐私性。
``` 

# HE-Booster: An Efficient Polynomial Arithmetic Acceleration on GPUs for Fully Homomorphic Encryption  

思路: 

  1. 使用CRT(中国剩余定理), 将 全同态加密（FHE）中的大模数(ciphertext)下的计算 转换成 对于小模数的计算, 放在GPU架构下进行并行.
  2. 注意: 需要将CRT与某个GPU特性操作结合, 或者(论文中)将CRT与其他优化算法联合结合, 否则可能太通用

# HEDA: Multi-Attribute Unbounded Aggregation over Homomorphically Encrypted Database

思路: 

  1. 论文中将 SQL过滤计算 拆成了 过滤操作 和 聚合操作, 有两种算法分别处理, 并在两种算法间使用了密文转换系统, 将过滤操作的结果密文转成了聚合操作的密文 (但缺少密文转换的细节)
